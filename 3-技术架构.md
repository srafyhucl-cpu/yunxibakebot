# 芸熙烘焙 AI 客服 — 技术架构

> 适用：开发者 核心问题：系统怎么搭？代码怎么写？怎么部署？

______________________________________________________________________

## 一、技术栈

| 组件 | 选型 | 说明 | |------|------|------| | 语言 | Python 3.11+ | AI 生态好，异步支持完善 | | Web 框架 | FastAPI |
异步、Pydantic 校验、自动文档 | | ASGI 服务器 | Uvicorn | 轻量高性能 | | 数据库 | SQLite (aiosqlite) | 文件级，零运维，WAL 模式 | |
LLM API | DeepSeek（兼容 OpenAI 协议） | 性价比高，中文强 | | LLM SDK | openai | DeepSeek 兼容 OpenAI SDK | | 有赞 API
| httpx | 异步 HTTP 客户端 | | 企微 API | httpx | 异步 HTTP 客户端 | | 反向代理 | Nginx | HTTPS 终结、静态资源 |

### 依赖清单

```text
fastapi>=0.110       # Web 框架
uvicorn[standard]    # ASGI 服务器
openai>=1.30         # DeepSeek/OpenAI SDK
aiosqlite>=0.20      # 异步 SQLite
httpx>=0.27          # HTTP 客户端（有赞/企微 API）
pydantic>=2.7        # 数据校验
pydantic-settings>=2.3  # 环境变量配置
python-jose[cryptography]  # JWT（管理后台登录）
jinja2               # 管理后台模板
```

______________________________________________________________________

## 二、系统架构

```text
用户请求 (HTTPS) → hclstudio.cn:443
    ↓
Nginx（反向代理 + SSL 证书）
    ↓
FastAPI (Uvicorn, 127.0.0.1:7001)
    │
    ├─ 接收消息
    │   有赞 Webhook ← 小程序
    │   企微回调   ← 1对1/群
    │
    ├─ 处理消息
    │   session_manager (会话 + 滑动窗口)
    │   llm.client (DeepSeek 调用)
    │   llm.functions (Function Calling 分发)
    │   knowledge_retriever (知识库检索)
    │   transfer_manager (转人工)
    │
    ├─ 发送回复
    │   有赞 API → 小程序
    │   企微 API → 客户微信
    │
    └─ 管理后台
       login/dashboard/transfers/reply
    │
    └── SQLite (data/bot.db)
```

______________________________________________________________________

## 三、核心对话流程（代码）

```python
# app/service/chat.py

async def handle_message(channel, user_id, content, staff_id="", raw_msg_id=""):
    """处理用户消息的主入口"""

    # 1. 去重（防止回调重复推送）
    if await message_repo.exists(raw_msg_id):
        return

    # 2. 获取或创建会话
    session = await session_repo.get_or_create(user_id, channel, staff_id)

    # 3. 保存用户消息
    await message_repo.save(Message(session.id, "user", content))

    # 4. 判断会话状态
    if session.status in ("transfer_pending", "human_service"):
        await handle_human_mode(session, content)
        return

    # 5. AI 对话循环（最多 3 轮 function call + 1 轮回复）
    for round in range(4):
        # 构建上下文：系统提示词 + 历史消息（滑动窗口 16K tokens）
        context = await build_context(session.id)
        # 调用 DeepSeek
        response = await llm_client.chat(context.messages, tools=FUNCTIONS)

        if response.finish_reason == "stop":
            # 纯文本回复 → 直接发送
            await message_repo.save(Message(session.id, "assistant", response.text))
            await send_reply(channel, user_id, response.text)
            return

        elif response.finish_reason == "tool_calls":
            # 工具调用 → 逐个执行
            for tool_call in response.tool_calls:
                result = await dispatch_tool(tool_call, session, user_id)
                await message_repo.save(Message(session.id, "tool", result,
                    tool_name=tool_call.function.name))
                if tool_call.function.name == "transfer_to_human":
                    return  # 转人工后结束循环

    # 超 3 轮 → 兜底
    await message_repo.save(Message(session.id, "assistant", fallback))
    await send_reply(channel, user_id, fallback)
```

### 滑动窗口算法

```python
def build_history(session_id, budget=16000):
    """从最新消息往回取，累计 token ≤ budget"""
    all_msgs = message_repo.get_all(session_id)
    selected, tokens = [], 0
    for msg in reversed(all_msgs):
        t = msg.estimated_tokens or estimate_tokens(msg.content)
        if tokens + t > budget:
            break
        selected.append(msg)
        tokens += t
    selected.reverse()  # 转回正序
    return selected
```

### Function Calling 分发

```python
FUNCTIONS = [...]

async def dispatch_tool(tool_call, session, user_id):
    name = tool_call.function.name
    args = json.loads(tool_call.function.arguments)
    match name:
        case "get_order_info":      return await youzan_client.get_order(args["order_no"])
        case "get_product_info":     return await knowledge_retriever.search(args.get("query", ""))
        case "get_logistics_info":   return await youzan_client.get_logistics(args["order_no"])
        case "get_refund_info":      return await youzan_client.get_refund(args["order_no"])
        case "search_knowledge":     return await knowledge_retriever.search(args["query"])
        case "transfer_to_human":    return await transfer_manager.create(session, user_id, args["reason"])
```

______________________________________________________________________

## 四、Function Calling 工具定义

DeepSeek 通过以下工具实现自主操作：

| 工具名 | 功能 | 触发场景 | |--------|------|---------| | `get_order_info` | 查订单详情 | "我的订单到哪了" | |
`get_product_info` | 查商品信息 | "提拉米苏多少钱" | | `get_logistics_info` | 查物流进度 | "什么时候送到" | |
`get_refund_info` | 查退款进度 | "我要退款" | | `search_knowledge` | 搜知识库 | 内部自动调用 | | `transfer_to_human` |
转人工 | "转人工"/"投诉" |

______________________________________________________________________

## 五、数据库设计（SQLite）

### sessions（会话表）

```sql
CREATE TABLE sessions (
    id TEXT PRIMARY KEY,
    channel      TEXT NOT NULL,       -- youzan / wecom_1on1 / wecom_group
    user_id      TEXT NOT NULL,
    staff_id     TEXT DEFAULT '',     -- 负责员工ID
    status       TEXT DEFAULT 'active',
                   -- active / transfer_pending / human_service / closed
    extra_info   TEXT DEFAULT '{}',   -- JSON
    created_at   TEXT DEFAULT (datetime('now')),
    updated_at   TEXT DEFAULT (datetime('now'))
);
CREATE INDEX idx_sessions_user ON sessions(user_id);
```

### messages（消息表）

```sql
CREATE TABLE messages (
    id TEXT PRIMARY KEY,
    session_id   TEXT NOT NULL REFERENCES sessions(id),
    role         TEXT NOT NULL,       -- user / assistant / system / tool
    content      TEXT NOT NULL,
    channel_msg_id TEXT DEFAULT '',   -- 渠道消息ID（去重）
    estimated_tokens INTEGER DEFAULT 0,
    tool_calls   TEXT DEFAULT '[]',   -- JSON
    tool_name    TEXT DEFAULT '',
    created_at   TEXT DEFAULT (datetime('now'))
);
CREATE INDEX idx_messages_session ON messages(session_id);
```

### knowledge_base（知识库）

```sql
CREATE TABLE knowledge_base (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    category TEXT NOT NULL,           -- store_info / product / policy / faq / after_sales
    title    TEXT NOT NULL,
    content  TEXT NOT NULL,
    keywords TEXT DEFAULT '',
    priority INTEGER DEFAULT 0,
    is_active INTEGER DEFAULT 1,
    created_at TEXT DEFAULT (datetime('now'))
);
CREATE INDEX idx_kb_category ON knowledge_base(category);
```

### human_transfers（转人工记录）

```sql
CREATE TABLE human_transfers (
    id TEXT PRIMARY KEY,
    session_id TEXT NOT NULL REFERENCES sessions(id),
    user_id    TEXT NOT NULL,
    staff_id   TEXT DEFAULT '',
    reason     TEXT DEFAULT '',
    status     TEXT DEFAULT 'pending', -- pending / accepted / closed
    conversation_summary TEXT DEFAULT '',
    created_at TEXT DEFAULT (datetime('now')),
    accepted_at TEXT,
    closed_at  TEXT
);
```

### orders（订单表）

```sql
CREATE TABLE orders (
    id TEXT PRIMARY KEY,
    session_id   TEXT NOT NULL REFERENCES sessions(id),
    channel      TEXT NOT NULL,
    user_id      TEXT NOT NULL,
    products     TEXT NOT NULL,        -- JSON: [{name, qty, spec, price}]
    total_amount REAL DEFAULT 0,
    delivery     TEXT DEFAULT '{}',    -- JSON
    status       TEXT DEFAULT 'pending',
                   -- pending / confirmed / making / delivering / done / cancelled
    remark       TEXT DEFAULT '',
    created_at   TEXT DEFAULT (datetime('now'))
);
```

______________________________________________________________________

## 六、API 端点

| 方法 | 路径 | 说明 | |------|------|------| | POST | `/api/v1/webhook/youzan` | 有赞消息回调（签名验证 + 消息处理） | |
POST | `/api/v1/webhook/wecom` | 企微消息回调 | | GET | `/health` | 健康检查 | | POST | `/api/v1/admin/login`
| 管理后台登录 | | GET | `/api/v1/admin/dashboard` | 控制台概览 | | GET | `/api/v1/admin/transfers/pending` |
待处理转人工列表 | | POST | `/api/v1/admin/transfers/:id/accept` | 客服接单 | | POST |
`/api/v1/admin/transfers/:id/close` | 客服结单 | | POST | `/api/v1/admin/sessions/:id/reply` | 客服回复消息 |

______________________________________________________________________

## 七、消息类型与过滤

### 消息过滤规则

收到消息后先做类型判断，不是所有消息都丢给 AI：

```text
消息进入
  ├─ text              → ✅ 直接交给 AI 处理
  ├─ image             → AI 回复"请发文字描述，我看到后会帮您处理~"
  ├─ voice             → 🎤 标记为语音来源，调 ASR 转文字后给 AI（后期接入）
  ├─ voice_call        → ❌ 跳过，客服已直接接听
  ├─ video_call        → ❌ 跳过，客服已直接接听
  └─ system_event      → ❌ 跳过（撤回/已读/系统通知等）
```

### 语音消息处理方案（后期）

```text
用户发语音
  → 平台推送语音文件 URL
  → 服务器下载音频
  → 调腾讯云/阿里云 ASR 转文字
  → 结果标记 msg_source="voice_asr" + 置信度
  → 文字内容交给 LLM
```

**管理后台显示标记：**

```text
🎤 语音消息 | 置信度 92%
   转写: "来个6寸草莓蛋糕"
   📎 点击回听语音
```

**客服可点击原始语音链接跳转到企微/有赞后台回听**，避免转写错误导致误解。

### 消息模型预留字段

```python
class Message:
    msg_source: str = "text"        # text / voice_asr / image
    voice_url: str = ""            # 原始语音文件 URL
    asr_confidence: float = 0.0    # 转写置信度
```

______________________________________________________________________

## 八、模块结构

```text
yunxi-bake-bot/
├── app/
│   ├── main.py                  # FastAPI 初始化、lifespan
│   ├── config.py                # 配置（pydantic-settings）
│   ├── database.py              # SQLite 初始化 + WAL + 建表
│   │
│   ├── models/                  # Pydantic 数据模型
│   ├── repository/              # SQLite 数据访问
│   │   ├── session_repo.py
│   │   ├── message_repo.py
│   │   ├── knowledge_repo.py
│   │   ├── transfer_repo.py
│   │   └── order_repo.py
│   │
│   ├── service/
│   │   ├── llm/
│   │   │   ├── client.py        # DeepSeek API 调用
│   │   │   ├── functions.py     # Function Calling 定义 + 分发
│   │   │   └── prompt.py        # System Prompt 构建
│   │   ├── youzan/
│   │   │   ├── webhook.py       # 签名验证 + 消息解析
│   │   │   └── client.py        # 有赞 REST API
│   │   ├── wecom/
│   │   │   ├── callback.py      # 回调消息解析
│   │   │   └── client.py        # 企微 API
│   │   ├── chat.py              # 核心对话循环
│   │   ├── session_manager.py   # 会话管理 + 滑动窗口
│   │   ├── transfer_manager.py  # 转人工管理
│   │   └── knowledge_retriever.py
│   │
│   ├── api/
│   │   ├── webhook.py           # POST /api/v1/webhook/*
│   │   └── admin.py             # 管理后台 API
│   │
│   └── templates/admin/         # 管理后台模板
│
├── data/                        # SQLite 文件（gitignored）
├── .env                         # 环境变量
├── requirements.txt
└── README.md
```

______________________________________________________________________

## 八、部署方案

### 域名设计

```text
hclstudio.cn（主域名，已备案）
├── yunxi.hclstudio.cn    → 芸熙烘焙管理后台   ← 当前
├── shop2.hclstudio.cn    → 未来第二家店       ← 扩展预留
└── hclstudio.cn/api/*    → API 通用入口
```

### DNS 配置

在域名服务商（阿里云/腾讯云）添加子域名解析：

| 记录类型 | 主机记录 | 记录值 | 说明 | |---------|---------|--------|------| | A | `yunxi` | 服务器公网 IP | 免费，秒级生效
|

### Nginx 配置

主域名和子域名共用一套 Nginx，通过 `server_name` 区分：

```nginx
# 主域名（API + 静态资源）
server {
    listen 443 ssl;
    server_name hclstudio.cn;

    ssl_certificate     /etc/letsencrypt/live/hclstudio.cn/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/hclstudio.cn/privkey.pem;

    location / {
        proxy_pass http://127.0.0.1:7001;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

# 芸熙烘焙管理后台
server {
    listen 443 ssl;
    server_name yunxi.hclstudio.cn;

    ssl_certificate     /etc/letsencrypt/live/hclstudio.cn/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/hclstudio.cn/privkey.pem;

    location / {
        proxy_pass http://127.0.0.1:7001;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
```

> HTTPS 证书使用通配符或 SAN 证书覆盖 `hclstudio.cn` 和 `*.hclstudio.cn`

### systemd 服务

```ini
[Unit]
Description=Yunxi BakeBot AI Service
After=network.target

[Service]
Type=simple
User=bot
WorkingDirectory=/opt/yunxi-bake-bot
ExecStart=/usr/local/bin/uvicorn app.main:app --host 127.0.0.1 --port 7001
Restart=always

[Install]
WantedBy=multi-user.target
```

### 启动自愈与向量校准架构 (Startup Lifespan Calibration)

系统在 lifespan 阶段加载了 BGE 密集向量模型，以实现高精度的本地 RAG 检索：

1. **加载模型**：在 CPU 上载入本地轻量级的高性能中文 Embedding 向量模型 `BAAI/bge-small-zh-v1.5`。
1. **拉取数据并解算**：自动从 SQLite 数据库的 `knowledge_base` 表中拉取所有在售 SPU（目前为 328 条活跃产品与服务）的物理行，转换成结构化 Markdown
   文本，逐一利用模型解算出 1024 维的高密 NumPy 实数特征矩阵。
1. **原子落库对齐**：通过多线程原子级写锁操作，同步将内存中的解算向量刷新并持久化到本地物理缓存，重构特征检索索引树，彻底杜绝数据库异动与向量矩阵的不一致。

### 自动化热部署流水线 (Automated Git Bundle Pipeline)

系统开发收尾并提交至本地 Git 后，通过高安全、极简的 **Git Bundle 物理合流** 流水线部署：

1. **打包增量**：本地执行 `git bundle create server.bundle HEAD`，将开发修改打包为标准二进制增量。
1. **无损合流**：通过 OpenSSH 的 `scp` 通道，秒级将 Bundle 推送至云服务器，并由 SSH 协力执行 `git fetch -f server.bundle HEAD`
   与强制硬指向 `git reset --hard`。
1. **零秒热重启**：最后通过 `systemctl restart yunxibakebot` 自动触发后台重启自愈，从而在极短时间内拉起新版本。

### 资源估算

| 项目 | 估算 | |------|------| | 服务空闲内存 | ~80 MB | | 服务负载内存 | ~200 MB | | 并发 LLM 调用 | 最大 5 个（信号量控制） | |
SQLite 年增长 | ~100-200 MB |

______________________________________________________________________

## 九、分阶段实施

| 阶段 | 内容 | 工时 | |------|------|------| | 1 | 项目骨架：FastAPI + SQLite + 配置 + 健康检查 | 1 天 | | 2 |
核心对话：DeepSeek + Function Calling + 会话管理 | 2-3 天 | | 3 | 有赞对接：Webhook + 消息发送 + 订单查询 | 2 天 | | 4 | 转人工
\+ 管理后台 | 2 天 | | 5 | 企微对接 + 冲突处理 | 2 天（等客户迁移） | | 6 | 知识库填充 + 部署上线 | 1 天 |

> 阶段 1-4 完成后即可上线小程序 AI 客服，总计约 7-9 天。
