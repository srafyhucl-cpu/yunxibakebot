# 外部门禁裁决

> 日期：2026-08-11 ｜ 基线：0.109.23 / d7e5e58
>
> 背景：多项计划长期被外部条件 blocked，完成定义被不可达条件绑架。本次对全部外部门禁做一次性裁决——降级交付、移出范围、或保持暂停，让计划可以真正关闭。

---

## 裁决清单

### 一、支付/退款

| 项 | 裁决 |
|---|------|
| WP4 Step 3 真实微信支付/退款（无商户号） | **降级交付**：支付改为 mock + 到账人工确认 + 产品说明文档。真实商户号到位后独立切片。 |
| WP6 completed handoff 依赖支付证据 | **完成定义改写**：支付降级后，WP6 完成条件不再要求真实支付证据。降级交付执行并登记即算完成。 |

### 二、容器/服务器

| 项 | 裁决 |
|---|------|
| R4-C Trivy 零漏洞硬门 | **已裁决为残余风险**。GitHub Actions CI 承载 build+smoke+scan 证据。 |
| 生产服务器跑 Docker build/smoke | **移出范围**。CI 由 GitHub Actions 承担，服务器只管部署。 |
| 生产独立备份磁盘 | **已裁决为降级**。本地 D 盘 AES-256-GCM 加密备份（已验证解密 + 完整性）。 |
| 根盘容量 40G/83% | **保持观察**。建议 `/ready` 加磁盘探针。 |

### 三、LangChain / AI 灰度

| 项 | 裁决 |
|---|------|
| LangSmith 外发灰度（合规+key） | **移出范围**。投入产出比不对项目的业务规模，不做。 |
| 真实脱敏回放 P17b | **移出范围**。依赖 LangSmith 链路，一并划掉。 |
| A1 混合检索灰度 | **降级启动**：先 shadow compare 观察，再决定灰度开。不受外部门禁阻塞。 |
| planned-hybrid/rerank | **保持关闭**：当前数据不支持。 |

### 四、文档/计划负债

| 项 | 裁决 |
|---|------|
| post-p0 计划 WP0/1/2 已执行未勾选 | **直接执行**：同步勾选。 |
| 进度清单版本号不一致 | **直接执行**：更新顶部版本号。 |
| wecom-closure-plan 3 条文档收尾 | **直接执行**。 |
| wecom-dev-plan 15 条未勾（确定性回复重构） | **已执行**：标记计划已完成。 |
| 上线检查清单 3 条（企微注册） | **已执行**：标记已完成。 |
| MiMo LLM 回调探针偶发 ReadTimeout | **降级**：从阻塞改为观察项，只登记不阻断。 |

### 五、业务价值（不受外部阻塞，待启动）

| 项 | 裁决 |
|---|------|
| customer master v1 生产正式导入 | **新计划**：独立执行（备份→dry-run→写入→对账）。 |

---

## 裁决后效果

外部依赖仅剩 3 条——且均非技术阻塞：

| 剩余依赖 | 性质 |
|---|---|
| 微信支付商户号 | 客户业务决策 |
| 服务器根盘容量 | 持续观察 |
| planned-hybrid/rerank | 等数据支撑 |

LangSmith 合规、脱敏回放、Trivy 零漏洞、独立备份盘——全部裁决完毕。

WP6 不再被支付证据绑架，可以收口。
