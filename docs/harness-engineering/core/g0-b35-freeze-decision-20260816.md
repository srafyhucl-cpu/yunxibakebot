# G0：B3.5 冻结基线确认与 D1 纵向切片路线（决定记录）

- trace_id: 20260816-g0-freeze-review-b35
- generated_at: 2026-08-16
- 决定类型: 冻结基线确认 / master 快进（代码基线决策，不部署）/ D1 开发路线与批准范围
- 相关证据: E-20260815-009（`docs/harness-engineering/core/evidence-index.md`）；复核报告 `reports/harness/g0-freeze-review-b35-20260816.md`（gitignored）

## 一、G0 冻结复核结论

- 对 B3.5 冻结整改包（内容提交 `6523b5f3779ff17e6fc5d64043c537f031882f6b` + 证据归档 `b30b2066ac27ef2326edae01240ab33882a3bf6e`）执行**一次**冻结清单复核，范围仅限既定 10 项收口与既定故障矩阵。
- 复核结果：**Go（通过）**——10 项全部核实落地（代码/测试/ADR 三通道），故障矩阵关键场景（券成功积分失败整体回滚、双连接恰一次结算、账户缺失/切换阻断、webhook 租约接管、AST 守卫负向、v3 报文正反向）全部可复现。
- **B3.5 为最后一个冻结整改基线**：不再扩写 B3.6 文档收敛轮次，后续方向切换为按纵向切片实现 D1。
- 记录偏差（无实质影响）：LOGBOOK B3.5 条目记 test_wechat_normalizers「16 项」，实际 15 函数 / 23 项收集（含参数化展开）。

## 二、评审 4 项问题的归属（均不阻断 B3.5）

1. 【高】D1 完整资金核心未实现（`payment_attempt` / `account_hold` / `accounting_outbox` / provider event / 退款额度 / 券投影）→ **D1-A 最小纵向切片**输入：mock 订单「预占→结算→取消释放→重放」。
2. 【高】积分退回/扣减仍按手机号执行，账户删除重建后可能写入新账户 → **D1-A**：不可变账户 ID 的 `get/credit/deduct`；账户缺失进 `manual_review`，禁止按手机号新建替代账户。
3. 【高】短缺债务与案件审计只有记录，无偿还/关闭/自动重开链路 → **D1-A**：债务部分偿还、`open→settled` 条件更新、后续积分入账优先偿债；`open_case()` 接入冲突重现路径。
4. 【中】时钟偏差：系统审计日期 2026-08-15，`6523b5f`/`b30b206` 的 Author/Commit 时间为 2026-08-16（本机时钟）。**处置：不 amend / 不重写已绑定 E-009 blob 的 B3.5 提交**；本记录与 `g0-freeze-review-b35-20260816.md` 为时钟偏差的正式留档；后续审批记录继续明确时钟偏差与可信时间源；B3.5 提交不得作为「截至 2026-08-15 已归档」的时间性证据。

## 三、批准范围（项目负责人确认，2026-08-16）

- **master 双远端快进**至 `b30b2066ac27ef2326edae01240ab33882a3bf6e`（代码基线决策）：不部署、不开放资产写操作、不批准 D1 上线、不开放真实用户。
- **B3.5 冻结基线生效**：审阅分支与 master 此后以 B3.5 为共同基线。
- **D1 开发测试**：方向获批准（按纵向切片），但 **D1-A 立项与启动待下一次明确指示**（本次不开始）。
- 原则：以后每个切片只批准「开发测试能力」，合并 / 部署 / 真实支付 / 真实用户开放 / 权威切换各自独立 No-Go。

## 四、D1 纵向切片路线（定稿）

```text
G0：B3.5 固定清单复核（一次，不扩范围）✅ 本记录
  ↓
master 快进（不部署）✅ 本记录
  ↓
D1-A：payment_attempt / hold / 单一 UoW / mock 结算（预占→结算→取消释放→重放）
  ↓
D1-B：积分与券接入统一支付应用服务（RESERVE / RELEASE / CONSUME / BACK + 投影 CAS；POINTS_DEDUCTION_FENCE 保持开启）
  ↓
D1-C：退款聚合、短缺债务、Provider inbox / outbox、预下单、关单、逐腿额度
  ↓
CT-1A → FP-4A 体验版 → CT-1 受控测试门禁
  ↓
商户条件具备后 FP-3b 受控真实支付/退款
  ↓
FP-1 正式导入 → FP-4B2 真实用户开放 → FP-2 权威切换（2027 窗口，先积分后券）
```

- 验收红线（D1-A）：预占后失败、结算重放、取消/超时释放、双连接单主体竞争、账户删除重建、短缺补足结案必须全部可复现。
- `POINTS_DEDUCTION_FENCE = True` 在 D1-B 完成前保持开启。
- 2027-05-31 前仅开发测试边界不变。

## 五、本提交范围

- 新文档：本文件（G0 决定记录）。
- 追加：`LOGBOOK.md`（G0 条目）、`项目进度与配置清单.md`（G0 条目）。
- 不改动任何 B3.5 提交内容（`6523b5f` / `b30b206` 各文件 blob 保持原样，E-009 绑定有效）。
- VERSION 保持 0.132.9（无代码变更，sync-version 不递增）。
