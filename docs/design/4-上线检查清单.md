# Bakery Commerce Platform — 风险评估与上线检查清单

> 说明：本文件是早期上线评估和实施清单，保留了当时对小程序、企微和域名部署的阶段性判断。当前如果要判断系统边界或仓职责，请优先参考 `docs/architecture/project-boundaries.md`。

> 发布时间评估、合规风险、成本估算、行动路径 域名: `yunxifood.cn`（已备案）

______________________________________________________________________

## 一、核心结论

| 事项 | 结论 | 说明 | |------|------|------| | **ICP 备案** | ✅ **已有** | `yunxifood.cn` 已备案，直接使用 | |
**软件著作权** | ❌ **不需要** | 自用系统不上架应用商店，无需软著 | | **企业微信认证** | ⏳ **第二阶段需要** | 300 元/年，迁移客户到企微时才需办理 | |
**有赞开发者认证** | ✅ 已有 | 已开通，无额外成本 | | **数据安全** | ⚠️ 需要注意 | 私政策中说明 AI 客服的对话数据用途 |

______________________________________________________________________

## 二、域名部署方案

已有已备案域名 `yunxifood.cn`，可以直接使用子域名或路径：

```text
API 服务（Webhook 接收 + 管理后台）
     ↓
yunxifood.cn 解析到服务器公网 IP
     ↓
通过 Nginx 反代到 FastAPI 服务
```

### 推荐方案

| 用途 | 地址 | 说明 | |------|------|------| | 有赞 Webhook 回调 |
`https://yunxifood.cn/api/v1/webhook/youzan` | 有赞云配置消息推送 | | 管理后台 | `https://admin.yunxifood.cn` |
浏览器打开即可使用 | | 健康检查 | `https://yunxifood.cn/health` | 监控服务状态 |

### HTTPS 配置

有 HTTPS 证书：

- 已有备案域名，可以免费申请 Let's Encrypt 证书
- 或使用服务器自带面板的一键 SSL 配置
- Nginx 反向代理 + HTTPS 终结，后端走内部 HTTP 到 FastAPI

______________________________________________________________________

## 三、软件著作权

| 场景 | 需要软著？ | |------|-----------| | 自用的 AI 客服系统 | ❌ **不需要** | | 有赞代开发的小程序 | ❌ 不需要你提供软著 | |
以后上架应用商店 | ✅ 需要 | | 申请高新技术企业认定 | ✅ 需要 |

**结论：现阶段不需要，不影响上线。**

______________________________________________________________________

## 四、企业微信认证

| 项目 | 说明 | |------|------| | 企业微信注册 | 免费 | | 企业认证费用 | **300 元/年**（微信官方收取） | | 认证所需材料 | 营业执照、法人身份证 |
| 认证耗时 | 1-3 个工作日 | | 未认证的限制 | 外部联系人上限 200 人，无法使用客户联系 API |

**建议：决定迁移客户到企微时再认证，不急。**

______________________________________________________________________

## 五、数据安全与合规

| 风险点 | 说明 | 应对措施 | |--------|------|---------| | **客户聊天记录存储** | SQLite 存了对话历史 | 服务器做好安全配置、防火墙、非
root 运行 | | **数据传输** | DeepSeek API 传输对话内容 | HTTPS 传输，不走明文 | | **有赞数据** | 查询订单信息 | 有赞 API 本身 HTTPS |
| **企微数据** | 收发消息 | 企微 API 强制 HTTPS |

**隐私政策建议：** 在小程序/企微的隐私条款里加一句：

> "本服务使用 AI 技术辅助客服回复，对话内容仅用于提升服务质量，不会用于其他目的。"

______________________________________________________________________

## 六、技术风险清单

| # | 风险 | 概率 | 影响 | 应对方案 | |---|------|------|------|---------| | 1 | **DeepSeek API 故障/超时** | 低 |
AI 无法回复 | 兜底回复 + 支持切换备用模型（通义千问） | | 2 | **DeepSeek API 响应慢** | 中 | 用户等待 | 设置 30 秒超时，超时告知"正在查询中" | |
3 | **有赞 Webhook 推送失败** | 低 | 收不到消息 | 有赞自动重试 3 次 | | 4 | **3M 带宽瓶颈** | 中 | LLM 回复为 JSON 文本，影响极低 |
限制并发 5 个 LLM 请求 | | 5 | **SQLite 并发写入锁** | 低 | 消息保存失败 | WAL 模式 + busy_timeout=5000 | | 6 | **服务器宕机**
| 低 | 服务不可用 | systemd 自动重启 + 监控脚本 | | 7 | **有赞 API token 过期** | 低 | 发送回复失败 | 自动刷新机制 | | 8 | **Nginx
配置错误** | 低 | 域名无法访问 | 配置前测试，保留 IP 直连作为备用 |

______________________________________________________________________

## 七、成本估算

### 固定成本

| 项目 | 金额 | 说明 | |------|------|------| | 服务器 | 已有 | 2核2GB，已付费 | | 域名 | 已有 | `yunxifood.cn` 已备案，续费约
60 元/年 | | HTTPS 证书 | 免费 | Let's Encrypt / 云平台免费证书 | | 企业微信认证（可选） | 300 元/年 | 第二阶段再支付 | | **总计** |
**0 元（启动）** | |

### 运营成本（按用量）

| 项目 | 估算用量 | 月费用估算 | |------|---------|-----------| | DeepSeek API | 每天约 500 次对话，每次约 1500 tokens |
**~30-50 元/月** | | 服务器 | 已包含 | 0 元 |

______________________________________________________________________

## 八、各渠道上线要求

### 第一阶段：小程序 AI 客服（早期阶段）

| 所需条件 | 状态 | 说明 | | :--- | :--- | :--- | | 域名 + ICP 备案 | ✅ 已有 | `yunxifood.cn` | | 有赞云开发者账号 | ✅ 已有
| - | | DeepSeek API Key | ✅ 已有 | - | | 服务器 Python 运行环境 | ✅ 已配置 | 生产环境已装配 Python 3.11 及独立 Venv | |
有赞消息推送配置 | ✅ 已配置 | 有赞开发者后台已成功订阅并配对公网 Webhook 回调 | | Nginx + HTTPS | ✅ 已切通 | `npm run check:production-domain` 已通过，`https://yunxifood.cn/health` 返回 200，根路径返回后台入口页面；Nginx 精确根路径重定向到 `/admin/`，其余 API 路径继续反代 FastAPI/Uvicorn 7001 |

**无损热部署通道、自愈自校准均已打通，小程序已进入高可用正式通航运行阶段。**

### 第二阶段：企微 1 对 1 + 群接龙（早期阶段）

| 所需条件 | 状态 | 成本/时间 | | :--- | :--- | :--- | | 企业微信注册 | ❌ 需注册 | 免费，1 天 | | 企业微信认证 | ❌ 需认证 | 300
元/年，1-3 天 | | 客户迁移到企微 | ❌ 需推动 | 预计 1-2 个月 |

______________________________________________________________________

## 九、部署架构

```text
用户请求 (HTTPS)
    ↓
yunxifood.cn:443
    ↓
Nginx（反向代理）
├─ 静态文件 /admin/static
├─ SSL 证书终结
└─ 转发到 localhost:7001
    ↓
FastAPI (Uvicorn)
├─ /api/v1/webhook/youzan  ← 有赞回调
├─ /admin/*                 ← 管理后台
├─ /health                  ← 健康检查
└─ SQLite (data/bot.db)
```

______________________________________________________________________

## 十、行动清单

### 先做（不影响上线进度）

- [x] 确认有赞云后台是否支持 HTTPS 回调（完全支持有赞云安全回调）
- [x] 将 `yunxifood.cn` 解析、HTTPS 证书和根路径入口完整切通（`npm run check:production-domain` 已通过）
- [x] 确认服务器已装 Python 3.11+（生产运行 Python 3.11.8 环境）
- [x] 准备知识库内容：已完成 328 条活跃商品和 FAQ 深度灌录与对齐
- [x] DeepSeek API 充值（已完成账户充值与在线连通测试）

### 代码阶段做

- [x] 阶段 1-2：搭骨架 + DeepSeek 对话
- [x] 阶段 3：对接有赞消息推送与双轨高并发防线
- [x] 阶段 4：管理后台 + 转人工工单 + 微信高保真独立消息模拟器

### 后续（不影响第一阶段）

- [ ] 注册企业微信 + 认证（300 元/年）
- [ ] 推动客户从个人微信迁移到企微
- [ ] 开发企微渠道对接
