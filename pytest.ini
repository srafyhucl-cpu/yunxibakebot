[pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*
addopts = -q --cov=app --cov-report=term --cov-report=html --cov-report=xml --cov-fail-under=70
pythonpath = .
asyncio_mode = auto
markers =
    slow: 标记较慢的测试用例
    integration: 标记需外部依赖的集成测试
    e2e: 标记端到端测试
