# 芸熙烘焙 AI 客服 — 项目进度与配置清单

> 最后更新 2026-08-13 - 当前本地代码版本为 `0.114.0`。
> 金额换算统一（2026-08-13）：新增 `app/utils.yuan_to_fen`（Decimal 安全换算、非法值返回 0），订单序列化/微信预支付/储值支付/看板汇总/企微查询 5 处浮点元转分收敛；60 项相关测试全过、门禁全绿。
> 性能收口（2026-08-13）：延迟 LangChain 重依赖导入并轻量加载 ToolNode，客户图冷启动工具用例约 11.4s→0.67s；tests/service/agents 全过、ruff/check_project 门禁全绿。
> M2 储值余额闭环已上生产（2026-08-12）：部署 v0.112.0（e6bc534），v022 两表落库（schema_version=22）、/ready database_schema_ready=true、充值/余额/组合支付路由生效（公网 401 探测=已注册）。真实微信支付（商户号）后置，M5 前端待计划书排期。
> M2 储值余额闭环（2026-08-12）：v022 迁移落地 `stored_value_recharge` / `balance_ledger`（unique_id 幂等、amount_fen 带符号）；新增 `app/service/stored_value/` 域（充值 mock 确认幂等入账、全额余额支付防超扣、组合支付先扣余额置 partial 再走差额 mock/微信、取消/超时原路退款）；新增小程序充值/余额/余额支付/组合支付端点；订单域 partial 中间态与微信差额通知按 remainFen 校验；修复 stored_value↔order 循环导入。14 项储值测试 + 全套 1365 通过、覆盖率 82.54%、门禁全绿。真实微信支付（商户号）后置，M5 前端待计划书排期。
> 会员储值/积分/优惠券账务域 M1 数据底座（2026-08-12）：v021 迁移落地 `member_balance` / `points_ledger` / `coupon_inventory`；`event_member.py` 处理 SCRM_CUSTOMER_EVENT / POINTS / COUPON_CUSTOMER_PROMOTION / SCRM_CUSTOMER_CARD 四类事件（unique_id 与 id+status+mobile 幂等去重、券详情反查补全）；新增全量导入 Service 与 `scripts/import_member_loyalty.py`（dry-run/apply）；7 项新测试 + 全套 1348 通过、覆盖率 83.02%、门禁全绿；生产已部署 0.111.0（ecffa3b）、v021 三表落库（schema_version=21）、Webhook 会员路由生效。待真实店铺验证会员域 API 契约后执行生产全量导入，详见计划书 `docs/specs/2026-08-12-member-loyalty-storedvalue-plan.md`。
> openid 预导入策略收口（2026-08-12）：已决策不做对话报号绑定（可冒报他人手机号，身份安全风险）；未识别客户按新客处理、打开小程序静默识别；认证/资质到位后可选用微信官方手机号授权组件（getPhoneNumber）补强。
> openid 预导入生产执行完成（2026-08-12）：部署 0.110.0（c4faf22），生产全量命中率 73.87%（10,007/13,547），customer_identity_links 累计 10,011 条（verified/confidence=100、重复 openid=0、无孤儿）；导入前备份 `/opt/backups/yunxibakebot/bot_before_openid_preimport_20260812.db`。待同 appid 替换后真实识别验证。
> openid 预导入本地全量验证完成（2026-08-12）：修复 `query_user_info` 的 `result_type_list` 传参（字符串 → JSON 数组 [2]，此前被网关静默拦截返回空）；用户已配置 IP 白名单；本地生产库快照全量跑命中率 73.87%（10,007/13,547），10,011 条链接全部 verified/confidence=100、重复 openid=0。待生产小批预检后全量导入。
> openid 预导入脚本（2026-08-12）：新增 `scripts/preimport_youzan_customer_openids.py`（YouzanClient.query_user_info + Mock 仿真 + 幂等写入 customer_identity_links），支持 dry-run / --limit / --skip-linked；5 项新测试与 62 项回归全过。待生产小批验证命中率后全量。
> 有赞开放平台 openid 接入清单（2026-08-12）：商家有赞小程序主体为商家、有赞为第三方授权，接管走「同 appid 替换」；客户 CSV 全表扫描确认无 openid/unionid；已确认商家具备三个能力包；新增 `docs/architecture/youzan-openapi-customer-openid-access-runbook.md`，技术路径为 `youzan.users.info.query`（按手机号 + result_type_list=[2] 返回小程序 openid/unionid），待商家开发者资质与计费确认后写导入脚本预导入 `customer_identity_links`。
> customer master 质量收尾（2026-08-11）：21 条补强候选经核对为重复客户（18 同名 + 1 相似 + 2 不同），243 条异常手机号已安全归入弱身份（主档 phone 全合法）；用户选定保守方案，仅保留合并候选清单 `reports/customer-master-merge-candidates-20260811.json`，不自动合并。
> 有赞客户正式导入（2026-08-11）：customer master v1 四表已落生产，批次 `youzan-import-20260811-175957`，24,726 条（auto_merge 13,551 + new_master 11,175），integrity ok、重复手机号 0；导入前快照 `/opt/backups/yunxibakebot/bot_before_customer_import_20260811.db`。证据 `E-20260811-002`。
> 检索日志报表修复（2026-08-11）：`scripts/report_knowledge_retrieval_logs.py` 文本模式修复 `format(**item, query=query)` 双传 `query` 键导致的崩溃，改为 `row = dict(item)` 后覆盖 `query` 再 `format(**row)`；`tests/scripts/test_report_knowledge_retrieval_logs.py` 4 项全过。A1 混合检索灰度已在生产生效（`ENABLE_HYBRID_RETRIEVAL=true`），观察期命令恢复正常。

> 外部门禁裁决（2026-08-11）：WP4 Step 3 真实支付/退款降级为 mock + 到账人工确认，不再阻塞 WP6 收口；LangSmith 外发灰度及脱敏回放移出范围；R4-C Trivy、生产 Docker build 和独立备份磁盘维持已裁决状态。裁决文档见 [docs/superpowers/plans/2026-08-11-external-gate-rulings.md](docs/superpowers/plans/2026-08-11-external-gate-rulings.md)。WP4 Step 3 降级后，`20260807-post-p0-production-closure` WP6 整列车收口进入执行阶段。

> 项目场景适配（2026-08-10）：R4-C 不再把 Trivy 零漏洞当作当前环境不可运行的硬门，已通过 GitHub Actions 拿到 `332a14c / 0.109.22` 的精确 build、隔离 smoke、非 root 和 Trivy 证据，24 项 HIGH/CRITICAL 均无上游修复版本，记录为残余风险。WP5 按单服务器现状接受 D 盘 AES-256-GCM 加密备份，已验证 `bot_backup_20260809_193003.ybak` 可解密且 SQLite `integrity_check` 通过。WP4 的 DevTools CLI 可执行，但 `islogin` 为 `false`、`open`/`auto` 返回 code 10，需人工登录后继续，不能标记 DevTools smoke 通过。生产仍为 `0.109.16 / 51d315748b`，未部署。证据登记为 `E-20260810-004`。

> R4-C CI 进展（2026-08-10）：新增独立 GitHub Actions workflow，已拿到 `0.109.22 / 332a14c` 的真实镜像 build、隔离 `/health` `/ready`、非 root 与后台 dist 证据；Trivy 0.73.0 最终基线为 24 项 HIGH/CRITICAL 且全部无修复版本（23 项 Debian 13 trixie、1 项 ecdsa）。`cryptography`、`pyasn1` 有修复版本已完成升级，基镜像已切换到 `python:3.11-slim-trixie`，相比 bookworm 减少 1 项且 CRITICAL 从 6 降到 4。按项目场景，WP3 已记录为残余风险而不是零漏洞硬门；不生成 completed handoff。证据登记为 `E-20260810-003`。

> 当前计划状态（2026-08-10）：`20260807-post-p0-production-closure` 工作包 1/2 本地实现与自动化验证继续通过；MiniApp Windows release 脚本和 secret-hygiene 检查已收口。`npm run release:readiness` 真实结果为 `24/28`，剩余微信 DevTools 登录/触达扫描、生产后台浏览器登录和截图证据；工作包 3 R4-C 已按项目场景完成 CI 证据并记录残余风险，工作包 5 已按 D 盘加密备份方案完成验证，工作包 4 DevTools/支付/生产发布仍 blocked，不生成 completed handoff。证据登记为 `E-20260810-001` 和 `E-20260810-004`。

> 生产只读真值（2026-08-10）：生产 `yunxibakebot=active`、版本 `0.109.16`、commit `51d315748b`，`/health`、`/ready` 均 200；服务器有 Docker 29.3.1/Compose 但无 Trivy，根盘 40G 已用 83%；`/mnt/backup/yunxibakebot`、备份 key 和 backup cron 均不存在。工作包 3 因 Trivy 和磁盘余量 blocked；工作包 5 因无独立持久化备份挂载 blocked；工作包 4 未执行生产发布。证据登记为 `E-20260810-002`，本轮未修改生产。

> Harness 历史证据治理（2026-08-07）：证据索引中 18 条历史 UI smoke 记录共引用 20 个已不可取回的截图/JSON 文件，现统一标记为 evidence_status=retired，保留原始事实但不纳入当前发布门禁。检查器会单独统计 retired 数量，非法状态仍失败。

> 当前计划状态（2026-08-07）：`20260807-post-p0-production-closure` 工作包 0 已建立 Harness 快照，工作包 1 客户 Agent 订单归属隔离已完成本地实现与自动化负向验证，工作包 2 商品向量状态机已完成本地实现、对账重试、Webhook 失败传播和全量自动化验证；R4-C 最终容器证据、MiniApp 真实 DevTools smoke、真实支付/退款、生产部署和独立持久化备份仍未闭环。外部运行时证据缺失时保持 blocked，不以本地静态测试冒充通过；LangSmith/replay/RAG 扩大灰度继续暂停。

> 工作包 2 真值（2026-08-07）：`pending -> syncing -> success/failed` 状态机、revision 防旧 worker 覆盖、失败重试、下架向量删除、商品专用对账和静态合同均已通过；证据登记为 `E-20260807-006`。本地测试通过不等于 Docker、支付、MiniApp 或生产运行时通过。

> 外部门禁真值（2026-08-07）：工作包 3 因本机无 `docker`/`docker compose`/`trivy` blocked；工作包 4 因无微信开发者工具、真实支付/退款商户测试条件和生产发布验证条件 blocked；工作包 5 因未取得生产独立备份挂载与恢复证据 blocked。未安装工具、未访问生产、不把本地测试写成外部通过；证据登记为 `E-20260807-007`。

> 生产目录迁移（2026-08-07）：当前应用工作目录统一为 `/opt/apps/yunxibakebot`，本地 `server` Git 远端、SSH Git Bundle 发布、生产快照/加密备份、只读生产合同和生产同步交接均已同步该路径。`yunxifood.cn` 继续承载后端 API 与健康检查；`yunxi.hclstudio.cn` 是 Nginx 静态 Web 入口。Yunxi 未声明 bare Git 部署仓，故 `git push server` 只同步 Git，生产发布须显式执行 `bash scripts/deploy.sh`；本轮未执行服务器连接、发布或重启。

> P0 小程序鉴权闭环（2026-08-05）：`20260805-storefront-auth-contract` 已完成后端测试 fail-closed、登录到受保护订单接口合同，以及 MiniApp session store、无认证 transport、Bearer 注入、401 单次续登和静态合同收口。Bot 目标测试 `31 passed`；MiniApp `typecheck`、`check:miniapp`、`check:page-api-coverage`、`check:observability-contract` 均通过。真实 DevTools service smoke 仍受 DevTools 连接、微信账号和合法域名外部条件约束，未执行的运行时证据不作通过声明；支付取消与库存一致性继续作为下一独立 P0 切片。

> P0 支付、取消与库存一致性（2026-08-07）：`20260807-order-payment-consistency` 已完成条件状态迁移和 Unit of Work 收口。已支付订单不能被用户或后台取消；支付通知、mock 支付和未支付超时关闭不能覆盖取消或已处理支付状态；库存仅在成功取消/关闭迁移后释放。订单服务/API 目标测试 `46 passed`，Ruff、订单仓储事务门禁、`git diff --check` 和使用 `D:\Temp` 临时根目录的全量后端测试通过。默认 pytest 临时目录在 C 盘时，3 项本地备份测试按 C 盘禁止策略失败；该结果不属于订单改动回归。真实微信支付、生产数据库与退款流程未执行，不能以本地合同替代外部证据。

> R4-C Docker最终门禁（2026-07-13）：真实构建已修复后台dist缺失、CUDA依赖膨胀、可修复HIGH漏洞、无用编译工具和源码编译回退；官方PyPI慢链路中断暴露builder下载不可恢复，现以BuildKit pip cache mount保留已完成HTTP下载，runtime继续离线安装且不携带构建缓存。精确HEAD重建、隔离health/ready和Trivy复扫仍在执行，未通过前不标记全局目标完成。

> R4-C 删除红线收口（2026-07-13）：Dockerfile 已移除递归删除，改为 dependency-installer 阶段向 runtime 复制 site-packages；deploy.sh 已移除工作区递归清理，新增部署/容器删除边界合同测试。该修复提交后必须用新精确 SHA 重新 build/smoke/scan。

> R4-C readiness路径复验（2026-07-13）：`0.109.10` 镜像默认`EMBEDDING_INDEX_DIR=/app/data`导致隔离smoke `/health=200`、`/ready=503`；根因为索引基名解析到volume之外。已将Dockerfile、Compose和合同统一到`/app/data/embeddings`，需以新精确HEAD重新build/smoke/scan。

> R4-C漏洞复验（2026-07-13）：`0.109.11` 默认smoke已通过health/ready/version、非root、单worker和资源边界，但Trivy发现pip/setuptools vendor中的2个可修复HIGH；runtime已改为卸载四项安装工具，需新精确HEAD复建并复扫归零。

> 2026-07-11 风险整改优先级更新：全局审计确认两项 P0 阻断风险和多项 P1 生产风险，当前状态是“R0-A/R0-B/R0-C、R1-A、R1-B、R1-C 已完成本地实施，主计划未完成”。R1-A 已收口认证、资源归属、服务端定价和微信支付通知合同；R1-B 已收口订单首批 Unit of Work、仓库提交边界和故障回滚合同；R1-C 已收口后台短会话、Origin、向量接口鉴权、ASGI body cap、并发保护、登录/IP 限流、成本熔断、安全响应头和默认关闭 API 文档，并登记 Nginx 反向代理合同；生产代理应用与只读复验待授权。下一工作包为 R2-A 数据库原子幂等。执行统一从 `docs/architecture/global-risk-remediation-and-framework-convergence-plan.md` 进入；在消息可靠性、隐私和发布恢复门禁关闭前，暂停扩大 LangChain E1-E6 生产灰度。框架治理按 ADR 0005 执行：通用能力优先复用成熟框架，AI 应用层不长期保留 LangChain 与自研双轨，业务事实仍由领域层管理。

> R0 首批执行记录（2026-07-11）：R0-A 已恢复 `real_sample_ready` 误改并完成计划/ADR/Harness 追溯；R0-B 已把生产快照改为允许表/列正向导出并通过合同测试；R0-C 已修复测试收集、质量合约 fake embedding、重复合约执行和 CI 的不存在 seed/废弃 embedding/自动部署入口。该阶段 `check_project.py --skip-tests` 与标准 `python -m pytest tests/ -q` 均通过，coverage 为 `81.52%`。


> 本轮生产收口：`090317ac9938ed9873998b24d5bd92e25257104b / 0.105.17` 已同步并重启；`/health`、`/ready`、运行时版本门禁、加强 release gate `8/8`、P13b 复核、生产容量、严格发布证据包、严格作品集工程证据和 P23a 外部证据交接汇总包均通过，`packet_ready=true`、`verified_evidence_ready=true`。E1-E5 外部证据仍未齐，`candidate_ready=false`、`real_sample_ready=false`、`shadow_log_ready=false`、`langsmith_enabled=false`、`external_evidence_complete=false`、`portfolio_complete=false` 不变。

> P19c 当前态：`scripts/build_rag_shadow_log_intake_packet.py` 已提供真实 RAG shadow log 仓库外交接模板、命令链和提交前自检清单；严格观测器要求真实来源类型、脱敏方法、审核人、ISO 审核日期、原始来源不入仓和 evidence ID，并拦截明显手机号、长数字与 open_id 形态。提交前自检要求外部日志持有人/脱敏审核人/仓库维护者确认真实来源、原始日志不入仓、query 脱敏、metadata 证明字段和 evidence ID 登记；模板生成不读取生产日志，当前无外部输入，`shadow_log_ready=false` 不变。

> P23a 当前态：`scripts/build_langchain_external_evidence_handoff_packet.py` 已把 E1 真实 replay 接入包、E2 真实 RAG shadow log 接入包和 E6 作品集缺口清单聚合成统一外交接汇总，输出 `required_external_inputs`、`missing_actions`、可填写模板、命令链、交接顺序和边界声明。该脚本只协调已有构建器，按“外部证据交接汇总”职责拆分，不读取真实客户记录或真实检索日志，不访问业务数据库，不调用外部 LLM，不修改生产服务，也不改变 `candidate_ready=false`、`real_sample_ready=false`、`shadow_log_ready=false`、`external_evidence_complete=false`、`portfolio_complete=false`。

> P17b-intake-precheck 当前态：`scripts/build_real_conversation_replay_intake_packet.py` 已新增 `pre_submission_checklist`，要求外部记录持有人/脱敏审核人/仓库维护者在真实 replay 提交前确认真实来源、原始数据不入仓、敏感字段脱敏、六类事实敏感场景覆盖目标和 evidence ID 登记；该清单不读取真实数据，不改变 `candidate_ready=false`、`real_sample_ready=false` 或外部完整态。

> P23b 当前态：`scripts/build_langchain_external_evidence_handoff_packet.py` 已在 `missing_actions` 之外新增 `action_groups.local_evidence_refresh_actions`、`action_groups.external_handoff_actions` 和带 owner / human_input_required 的 `action_group_details`，在 `--summary` 输出本地刷新与外部交接动作数量，并透传 P17b/P19c `pre_submission_checklist`；该分类不读取真实数据、不改变生产配置，也不改变任何外部 readiness。

> P23c 当前态：`scripts/build_langchain_external_evidence_handoff_packet.py` 已新增顶层 `pre_submission_checklist_summary`，聚合 P17b 真实 replay 与 P19c RAG shadow log 的提交前自检项，并在 `--summary` 输出 `precheck_items=10`；该摘要只提升外交接可读性，不读取真实数据，也不改变 `candidate_ready=false`、`real_sample_ready=false`、`shadow_log_ready=false`、`external_evidence_complete=false` 或 `portfolio_complete=false`。

> P23d 当前态：`scripts/check_langchain_ai_layer_production_plan.py` 已把 P23 交接合同字段纳入静态计划门禁，要求计划继续包含 `action_groups`、`action_group_details`、`pre_submission_checklist_summary` 和 `precheck_items=10`；该门禁只防止文档/计划回退，不执行生产访问、不接入真实外部数据、不改变任何 readiness。

> P23e 当前态：`scripts/build_langchain_external_evidence_handoff_packet.py` 已新增顶层 `readiness_truth`，集中输出 `candidate_ready=false`、`real_sample_ready=false`、`shadow_log_ready=false`、`external_evidence_complete=false`、`portfolio_complete=false`、E1-E5 阶段动作和 false reason；该摘要只复述当前真值，不改变任何 readiness。

> P23f 当前态：`scripts/build_langchain_external_evidence_handoff_packet.py` 已支持 `--markdown-out`，可在生成 JSON 汇总包的同时输出 `LangChain 外部证据交接包` Markdown 文档，集中展示 readiness 真值、动作分组、P17b/P19c 提交前自检摘要、真实 replay/RAG shadow log 交接命令、边界声明和下一道完整性门禁；该文档面向外部记录持有人、脱敏审核人和仓库维护者，不读取真实数据、不改变任何 readiness。

> 外部证据交接治理批量门禁：P17b-intake-precheck、P19c-intake-precheck、P23b、P23c、P23d、P23e 和 P23f 已按“完成一波集中检查”的节奏通过本地收口验证，包括 `python scripts/check_file_sizes.py`、`python scripts/check_project.py --skip-tests`、相关脚本测试、Ruff check / format、`python scripts/check_evidence_index.py --summary` 和 `git diff --check`。本批次未接入真实客服记录、未接入真实 RAG shadow log、未启用 LangSmith 外发，也未改变 `candidate_ready=false`、`real_sample_ready=false`、`shadow_log_ready=false`、`langsmith_enabled=false`、`external_evidence_complete=false` 或 `portfolio_complete=false`。
> 说明：本文件同时承载当前项目进度、生产同步清单和历史阶段记录。当前产品边界与双仓职责请以 `README.md` 和 `docs/architecture/project-boundaries.md` 为准；文中出现的“芸熙烘焙 AI 客服”更多指首个落地实例或历史阶段口径。

> P10b 当前态：`scripts/check_langchain_ai_layer_release_gate.py` 已支持显式 `--include-production-smoke`，可在默认 133 eval、客户 graph 回复回放 probe、扩展 163 eval 后追加生产 `/health`、`/ready` 和企微员工助手 callback probe；默认门禁仍不访问生产。`scripts/smoke_test.py --http-only` 只检查远程服务可达、`/health` 和 `/ready`，避免本地静态配置误判远程生产失败。历史试跑暴露的 `0.85.2` 版本漂移和 2 项 callback 语义失败已在 P14c 收口。

> P10c 当前态：release gate JSON 已新增 `release_summary`，可直接展示默认 Agent Eval 133/133、扩展 Agent Eval 163/163、RAG best mode 与 Recall@5/MRR，以及生产 smoke/callback 的核心状态和 failed names；摘要只引用已有报告，不改变 release gate 的通过判定。

> P11a 当前态：已新增脱敏真实会话 replay 数据契约和检查入口 `scripts/check_real_conversation_replay.py`，样例 fixture 不含真实客户原文；每条 replay case 必须绑定客户敏感 golden case，并复用 forbidden reply patterns 检查最终回复。聚合 Agent Eval 默认 133 项不变，显式 `--include-real-replay` 后当前为 135 项通过，同时包含 reply replay 与 real replay 后为 165 项通过。

> P11b 当前态：release gate 已支持显式 `--include-real-replay` 和 `--real-replay-fixture`，在默认 3 步门禁之外追加脱敏真实会话 replay 契约检查和包含 real replay 的聚合 Agent Eval；默认门禁仍为 3/3 通过，显式 real replay 门禁当前为 5/5 通过，摘要显示 real replay 样例 2/2、并入聚合 Agent Eval 后 135/135。

> P11c 当前态：已新增 `scripts/export_real_conversation_replay_fixture.py`，可将 JSON / JSONL 原始记录离线脱敏导出为 P11a fixture 草稿；每条记录必须显式绑定 `golden_case_id`，默认输出到 gitignored `reports/agent-eval/real-conversation-replay-draft.json` 并立即调用 replay checker。合成输入样例导出 2/2 通过，导出 draft 接入 release gate 后 5/5 通过。

> P11d 当前态：已新增 `scripts/check_real_conversation_replay_coverage.py` 和显式 release gate 参数 `--include-real-replay-coverage`，默认要求客户事实敏感场景每类至少 5 条 replay case。合成脱敏覆盖样例 30/30 通过 checker，覆盖率门禁 6/6 通过，release gate 强门禁 6/6 通过；该合成样例不等同真实客服样本池，后续真实脱敏样本接入时应复用同一门禁。

> P12a 当前态：已新增 `scripts/check_langchain_ai_layer_production_plan.py`，并接入 `scripts/check_project.py --skip-tests`，把 `docs/architecture/langchain-ai-layer-production-enhancement-plan.md` 的持续执行状态、已完成阶段、下一步建议、真实样本边界和生产版本边界变成机械门禁；合成覆盖样例仍只用于验证门禁形状，不等同真实客服样本池。

> P12b 当前态：已新增 `scripts/check_real_conversation_replay_pool.py` 和样本池 manifest 样例，用机器门禁区分真实脱敏客服样本与合成覆盖样例；release gate 显式支持 `--include-real-replay-pool`，并可用 `--require-real-replay-pool` 阻断只有合成样例的“伪真实样本池”。当前样例 manifest 通过但 `real_pool_ready=false`，不能作为真实问题分布证据。

> P15a 当前态：真实 replay pool 的 `is_real_customer_data=true` 条目必须补齐 `source_type=real_customer_conversation`、`redaction_method`、`redaction_reviewer`、`redaction_reviewed_at` 和 `raw_source_retention=not_committed`，且 fixture metadata source 不能是合成样例来源。当前仓库仍未接入真实客服样本，`--require-real` 按预期失败。

> P16a 当前态：已新增 `scripts/check_langsmith_runtime_config.py`，可只读检查 LangSmith / LangChain tracing 运行时配置、API key 是否配置和 RunnableConfig metadata 脱敏结果；默认关闭态通过，`--require-enabled` 在当前缺少生产 LangSmith key/tracing 开关时按预期失败。`scripts/check_langchain_ai_layer_release_gate.py --include-observability-evidence` 已先运行 `langsmith_runtime_config`，再运行原有观测证据包。本片不向 LangSmith 外发、不调用外部 LLM、不读取业务数据库、不改变客户或员工热路径。

> P17a 当前态：已新增 `scripts/check_real_conversation_replay_intake_readiness.py`，可检查真实脱敏 replay 接入通道是否具备导出器、replay checker、coverage checker、pool checker 和 manifest 门禁；默认报告通过并输出 `real_sample_ready=false`，说明当前只有合成 contract pool，不能作为真实业务分布证据；`--require-real` 在当前无真实脱敏样本时按预期失败。release gate 显式支持 `--include-real-replay-intake-readiness`，`check_project.py --skip-tests` 已纳入默认业务合约。

> P17b-prep 当前态：已新增 `scripts/prepare_real_conversation_replay_pool_entry.py`，可把已脱敏且人工审核过的 replay fixture 生成真实样本池 manifest 条目草稿；脚本会检查 fixture 来源不是 synthetic/schema sample、`metadata.contains_sensitive_data=false`、脱敏证明字段、真实来源类型、原始来源不入仓声明和六类事实敏感场景覆盖。本片不读取原始客户会话、不修改 manifest、不访问业务数据库、不调用外部 LLM、不提交真实客户数据。

> P17b-candidate 当前态：已新增 `scripts/audit_real_conversation_replay_candidate.py`，可在真实 replay fixture 写入样本池 manifest 前做只读候选审计；默认无输入时通过但 `candidate_ready=false`，严格模式 `--require-fixture` 缺输入失败。有候选 fixture 时会复用 replay 与 coverage 门禁，并要求真实来源类型、脱敏方法、审核人、审核时间、原始来源不入仓声明和 evidence ID。

> P13a 当前态：已新增 `scripts/report_langchain_observability_evidence.py`，默认运行受控 fake trace probe，汇总双机器人 trace、LangSmith 配置脱敏状态和冷导入重依赖检查；release gate 显式支持 `--include-observability-evidence`，`check_project.py --skip-tests` 也已接入该业务合约。当前证明本地/准生产观测链路可用，不等同于 LangSmith 线上外发已启用。

> P13b 当前态：`scripts/check_langchain_production_observability_release.py` 用于复核显式生产 release gate JSON 是否足以作为上线收口证据；门禁会检查 release gate 顶层状态、生产 smoke、企微员工助手 callback probe、LangChain 观测证据包、LangSmith 开关显式记录、`langchain_ai_layer_capacity` 生产容量摘要，并从 `/health`、`/ready` detail 解析生产接口真实版本。当前最新生产报告已通过：production_versions=`0.105.9`，callback_failed=0，capacity_runtime=ok，LangSmith 仍为显式关闭态。

> P14a 当前态：已新增 `scripts/report_langchain_production_sync_handoff.py`，用于把 P13b 状态、本地目标 commit、`origin/master`、`server/master`、SSH 权限状态、人工动作和复验命令聚合成生产同步交接报告。当前 P14 handoff 已通过，生产 target commit 为 `579a4000a02634774bb3de64e2282351e79dd7cd`，blockers=0。

> P14b/P14c 当前态：生产已同步并重启到 `0.105.1`，公网 `/health`、`/ready` 运行时版本门禁通过；显式生产 release gate、P13b 发布证据门禁、P14 handoff 和 callback failure report 均通过，callback_failed=0、blockers=0。

> P14c 生产收口态：`scripts/report_langchain_production_callback_failures.py` 当前输出 `status=passed failed=0 runtime_status=passed app_version=0.105.1`；P14c 已从生产版本漂移和 callback 失败定位阶段收口，下一步转向 P17b 真实脱敏样本接入。

______________________________________________________________________

## 2026-07-09 商品 RAG 向量化动静分离
- [x] 已冻结 LangChain 生态全面接管 AI 应用层计划：`docs/architecture/langchain-ecosystem-ai-layer-takeover-plan.md` 明确第二轮目标不是让 LangChain 接管整个业务项目，而是接管 Agent 编排、模型调用、工具绑定、Retriever、Prompt、Memory / Checkpoint、Tracing 和 Eval；订单、商品、客户、售后、数据库和领域 service 继续保持现有分层。
- [x] 第二轮首个执行切片 `phase-1-langchain-customer-model-binding` 已完成首版：客户 `model_with_tools` 已从项目内 `request_llm_choice()` 迁向 `request_customer_model_with_tools()`，通过 LangChain `ChatOpenAI.bind_tools()` 绑定客户 `StructuredTool`；本片不改 RAG、Prompt、Memory 或业务 service。
- [x] 阶段 1 已补 LangChain 模型 adapter 测试和客户 graph 回归：覆盖 `bind_tools()`、OpenAI dict messages 到 LangChain messages 转换、LangChain 原生 tool call dict、fallback、旧 wrapper 退场保护、compiled graph 不复用旧 session 工具上下文。
- [x] 第二轮阶段 2 `phase-2-langchain-knowledge-retriever-adapter` 已完成最小首版：新增 `app/service/agents/rag/documents.py` 和 `retriever.py`，将 `KnowledgeEntry` 包装为 LangChain `Document`，并将现有 `KnowledgeRetriever.search()` 暴露为异步 LangChain `BaseRetriever`；本阶段不接入客户热路径，不改变 audience、发布状态、有效期、实时库存注入或知识检索日志。
- [x] 第二轮阶段 3 `phase-3-customer-prompt-components` 已完成最小首版：新增 `app/service/agents/customer/prompts.py`，将客户 system prompt、会话摘要只读注入、上下文 messages、商品标题提取和 guard source 构造拆为可测试组件；`chat_context.py` 继续作为热路径 facade，不改变 RAG 检索、context budget、图片消息处理或会话摘要读取策略。
- [x] 第二轮阶段 4 `phase-4-customer-memory-checkpoint-boundary` 已完成最小首版：新增 `app/service/agents/customer/memory.py` 聚合 active 会话摘要和已加载客户画像为只读 `CustomerMemoryBlock`；新增 `app/service/agents/checkpoints.py` 为客户 graph 调用生成稳定 `configurable.thread_id=customer:<session_id>`，并提供懒加载 `MemorySaver` helper；默认生产不启用持久 checkpoint，不改变摘要生成、长期画像写入或数据库 schema。
- [x] 第二轮阶段 5 `phase-5-agent-local-observability` 已完成最小首版：新增 `app/service/agents/observability.py`，客户 graph 和员工助手 graph 的 `trace_events` 统一通过 `AgentTraceEvent` / helper 生成，保留 `node` 顶层字段并新增 `event=node`；本阶段只做本地事件结构标准化，不启用 LangSmith 外发、不新增数据库写入。
- [x] 第二轮阶段 6 RAG Advanced 化已完成离线评测闭环：`LangChainKnowledgeRetriever` 支持 query planner 和可选 document reranker；`scripts/eval_retrieval.py` 支持 `planned-vector`、`planned-hybrid` 和显式 `--rerank`；`scripts/report_retrieval_eval_matrix.py` 可一条命令比较 `vector`、`hybrid`、`planned-hybrid`、`planned-hybrid+rerank`，且同轮只构建一次向量索引和一次 BM25 索引。
- [x] 第二轮阶段 7 员工助手 planner 已接入 LangChain structured output：新增 `app/service/agents/employee/prompts.py` 和 `structured_planner.py`，`EmployeeAgentPlanner` 仍保持 rule-first；LangChain structured planner 只替换弱关键词 LLM fallback，失败后回落旧 JSON fallback，最终回复继续由 deterministic finalizer 输出。
- [x] 第二轮阶段 8 已新增双机器人统一 Agent Eval：`app/service/agents/evaluation.py` 提供通用 eval 模型，`scripts/eval_customer_agent.py`、`scripts/eval_employee_agent.py` 和 `scripts/report_agent_eval.py` 可聚合客户 RAG golden cases、员工 planner 探针和能力合约；当前本地报告为 `agent_eval status=passed total=58 failed=0 pass_rate=1.0`。
- [x] 第二轮阶段 9 已完成文档、ADR 和作品集收口：`README.md`、`docs/README.md`、`docs/AGENTS/quick-reference.md`、`docs/architecture/bot-capability-matrix.md` 已更新当前 LangChain 生态化边界；新增 `docs/harness-engineering/adr/0003-langchain-ai-layer-boundary.md` 和 `docs/architecture/langchain-ai-layer-portfolio.md`，明确 LangChain 接管 AI 应用层但不接管订单、商品、客户、知识发布状态和数据库事实层。
- [x] LangChain AI 应用层生产增强 P0 已完成首轮生产验证：新增 `docs/architecture/langchain-ai-layer-production-enhancement-plan.md`，生产已同步到 `0.85.2 / 0fa3f18431` 并通过 `/health`、`/ready` 和员工助手 callback 48/48；随后将 callback 商品库存探针从固定库存数字改为动态库存治理口径，后续应进入 P1 线上 Trace / LangSmith 观测。
- [x] LangChain AI 应用层生产增强 P1a 已完成本地 Agent trace 报告切片：新增 `app/service/agents/trace_report.py` 和 `scripts/report_agent_traces.py`，可从指定 JSON 或 `reports/agent-traces/` 最新 JSON 汇总客户机器人和员工助手 trace；`observability.safe_trace_payload()` 已统一过滤 open_id、手机号、地址、token、密钥、消息原文、历史记录、客户画像和工具结果。本片不改 graph 热路径、不写业务表、不打开 LangSmith 外发。
- [x] LangChain AI 应用层生产增强 P1b 已完成 graph 显式 trace 导出：客户机器人和员工助手 graph service 均新增 `answer_with_trace()`，在保持原 `answer()` 字符串返回行为不变的前提下返回可序列化 `AgentTraceRun`；本片不默认写 `reports/agent-traces/`，避免生产热路径每条消息产生文件，后续显式探针、eval 或结构化日志可按需落盘并交给 `scripts/report_agent_traces.py` 汇总。
- [x] LangChain AI 应用层生产增强 P1c 已补齐 graph 节点 trace 字段：客户侧记录 RAG 耗时、知识 ID、知识命中数量、模型名、模型耗时、工具数量、工具名和 fallback reason；员工侧记录工具名、工具数量和 deterministic final status。字段只保留结构化元数据，不写用户原文、历史、客户画像、知识正文或工具结果明文。
- [x] LangChain AI 应用层生产增强 P1d 已完成本地 trace probe 闭环：`scripts/probe_agent_traces.py` 使用 fake 依赖运行客户 graph 和员工 graph，写入 gitignored `reports/agent-traces/agent-traces-{timestamp}.json`；`scripts/report_agent_traces.py --latest --summary` 已能看到 `agent_traces status=ok total_runs=2 agents=2`。本片不访问真实数据库、不调用外部 LLM、不发送企微消息。
- [x] LangChain AI 应用层生产增强 P2a 已完成 eval runner 参数与 JSON 归档能力：客户、员工和聚合 eval 均支持 `--case-id`、`--fail-fast`、`--json-out`，聚合报告额外支持 `--agent customer|employee|all`；`python scripts/report_agent_eval.py --latest --json-out reports/agent-eval/latest.json` 可归档当前 58/58 通过的双机器人 eval 报告。
- [x] LangChain AI 应用层生产增强 P3d 已完成 RAG shadow compare 显式运维探针：`scripts/report_retrieval_shadow_compare.py` 支持 `--baseline-mode` 和可重复 `--candidate-mode`，报告 metadata 记录当前 `RAG_RETRIEVAL_MODE`；真实 embedding 路径下 planned-hybrid 与 baseline 持平，planned-hybrid+rerank 仍低于 baseline，当前不应热启 rerank。
- [x] LangChain AI 应用层生产增强 P3e 已完成 RAG 检索模式热路径 feature flag：默认 `hybrid` 继续直接调用 `KnowledgeRetriever.search()`，`planned-hybrid` / `planned-hybrid-rerank` 才通过 LangChain retriever adapter 编排 query planning / rerank，并将 Document 还原为现有 `KnowledgeEntry` 供 prompt、guard 和 trace 复用。
- [x] LangChain AI 应用层生产增强 P4a 已完成客户事实敏感场景 eval 扩容：客户业务样本从 40 条提升到 70 条，并新增订单、退款、售后、库存、价格、转人工 6 类 `sensitive_scenarios` 覆盖检查，每类至少 5 条；双机器人聚合 eval 提升到 133 项通过。
- [x] LangChain AI 应用层生产增强 P4b 已完成事实敏感策略契约断言：客户 fixture 检查集中定义 6 类高风险场景 guardrail 关键词契约，客户 eval 对每条敏感 case 输出 `sensitive_policy.<scenario>` 断言，防止样本只标风险但没有写清回复边界。
- [x] LangChain AI 应用层生产增强 P4c 已完成事实敏感场景报告汇总：单 agent 和双机器人聚合 eval JSON 均输出 `sensitive_scenarios` 汇总，只包含场景标签、数量、失败数和通过率，可直接用于上线报告和作品集展示。
- [x] LangChain AI 应用层生产增强 P4d 已完成禁止回复模式输出契约：订单、退款、售后、库存、价格、转人工 6 类敏感场景按标签自动派生 `forbidden_reply_patterns`，客户 eval 增加 `forbidden_reply_patterns.present` 断言，为后续真实回复回放检测不当承诺或编造做准备。
- [x] 向量相似度契约已统一：`EmbeddingSearcher.search()` 不再走 sklearn `NearestNeighbors` cosine distance 分支，统一使用归一化文档向量与归一化 query 向量的 exact dot product，分数即 cosine similarity。
- [x] 已补向量数学回归测试：覆盖 search 分数、增量 upsert 归一化和历史缓存加载归一化，避免 ANN / exact 双路径分数语义再次漂移。
- [x] 商品 RAG 同步已拆分展示文本和 Embedding 静态语义文本：知识库正文继续保留库存展示和 UMP 商品卡，向量 upsert 使用商品名称、状态、规格、价格、定制项、标签、链接和描述，不再写入具体实时库存数字。
- [x] 命中后的实时事实仍由 `prepend_live_data()` 从 `youzan_products` 注入最新价格、库存和上下架状态，避免向量语义文本携带高频库存变动。
- [x] 已补回归测试证明商品 embedding 输入不含 `当前可用库存`、`当前可用总库存` 和具体库存件数，同时 `knowledge_base.content` 仍保留库存展示。

______________________________________________________________________

## 2026-07-08 LangChain / LangGraph 双机器人迁移
- [x] 已提交并推送阶段 1-3：`f7eb52b73b98a833052a3dfe5afeb45910df1af7` 已同步到 `origin/master` 和 `server/master`，版本递增到 `0.75.0`；内容包括 LangChain / LangGraph 依赖和容量探针、客户/员工双范围 LangChain tool registry、员工助手 LangGraph 接管。
- [x] 已完成阶段 4 首版：客户机器人 `run_ai_conversation_loop()` 已调用 `CustomerAgentGraphService`，customer graph 覆盖 `load_session_context -> model_with_tools -> execute_tools -> finalize_reply / tool_round_limit -> record_trace`；工具执行使用阶段 2 的 LangChain `StructuredTool` 注册表，转人工接回现有 `request_human_transfer`。
- [x] 已完成阶段 5 旧编排退场：客户工具执行不再经过 `dispatch_tool`，OpenAI tool schema 由 `app/service/agents/tools/customer.py` 统一生成，tool message 辅助迁入 `app/service/agents/customer/tool_messages.py`，旧 `chat_llm.py`、`chat_tools.py`、`app/service/llm/function_defs.py`、`app/service/llm/functions.py` 已删除。
- [x] 阶段 5 review 已补复用风险回归：`CustomerAgentNodes` 不再缓存带 session 的 customer tools；同一个 `CustomerAgentGraphService` 连续处理不同 session 时，工具上下文会按当前请求重建。
- [x] 生产同步兼容修复：生产 Python 3.10 不支持 `datetime.UTC`，已改为 `timezone.utc`，保持知识审核时间和知识检索报表生成时间的 UTC 语义。
- [x] 当前 LangChain / LangGraph 重依赖仍保持懒加载：干净进程导入 `app.main` 后 `langchain_core.tools=False`、`langchain_openai=False`、`langgraph=False`；容量探针显示 `langchain_openai` 冷导入仍是主要内存压力，阶段 4 本地 RSS 增量约 313.54MB，最小 graph 增量约 0.24MB。
- [x] 阶段 4 已通过客户侧主要验收：`tests/service/agents`、`tests/service/test_chat_refactor.py`、`tests/service/youzan`、客户 RAG golden cases、知识 audience smoke、知识检索日志 smoke 和 `check_project.py --skip-tests`。
- [ ] `scripts/eval_retrieval.py --fixture tests/fixtures/customer_rag_golden_cases.json` 仍缺 `data/prod_snapshot/eval.db` 本地语料库，需按脚本提示拉取生产评估快照后补证。
- [ ] 阶段 6 待执行：补观测、能力矩阵最终口径、迁移 ADR 和求职展示材料。

______________________________________________________________________

## 2026-07-07 当前代码文档同步
- [x] 已按当前代码反向同步项目文档入口：`README.md` 补充 `0.74.34` 当前代码事实、canonical 目录、API 入口和文档入口；`docs/README.md` 补充当前路由装配、服务装配和质量门禁事实。
- [x] 当前路由装配入口为 `app/lifespan_routes.py`：外部继续保留 `/api/v1/miniapp/*`、`/api/v1/admin/*`、`/api/v1/wecom/*`、`/api/v1/webhook/*`；真实前台渠道代码在 `app/api/channels/storefront/`，后台和集成真实 Router 分别在 `app/api/admin/`、`app/api/integrations/`。
- [x] 当前服务装配入口为 `app/lifespan_services.py`：核心 canonical 服务包括 `catalog_service`、`order_service`、`customer_address_service`、`customer_group_service`、`storefront_conversation_service`、`employee_agent_service`；旧 `miniapp_*` service key 作为兼容别名保留。
- [x] 当前统一质量门禁为 `python scripts/check_project.py --skip-tests`，发布前只读预检为 `python scripts/preflight_production.py --json`；业务合约覆盖员工助手能力、客户 RAG golden cases、知识治理、客户长期记忆、客户可观测、MiniApp 页面 API 和 GitHub 参考计划边界。
- [ ] 生产当前真实版本、企微群内人工验收、微信开发者工具体验版和真实支付验收仍属于外部状态，不能仅凭本地文档同步视为完成。

______________________________________________________________________

## 2026-07-06 GitHub 借鉴计划与客户上下文治理
- [x] 已冻结 GitHub 参考项目借鉴计划：客户机器人继续走 RAG + 受控工具 + 转人工，员工助手继续走确定性工具和模板化回复，LangChain / LangGraph 只允许作为离线或固定流程试点，不进入客户热路径或员工事实回复主链路。
- [x] 已落地双机器人能力目录：`docs/architecture/bot-capability-matrix.md` 区分客户机器人、员工助手、共享底层 service、回复策略和验证入口，避免后续工具混线。
- [x] 已建立客户 RAG golden cases：`tests/fixtures/customer_rag_golden_cases.json` 覆盖商品咨询、配送、退款售后和转人工，并接入 `scripts/check_customer_rag_golden_cases.py`、`scripts/eval_retrieval.py --fixture tests/fixtures/customer_rag_golden_cases.json` 和 `scripts/check_project.py --skip-tests` 的“业务合约检查”。
- [x] 已增加客户上下文预算观测：`timing.context_budget` 和 `reply_latency.meta_data` 记录最近消息、RAG、客户画像、system prompt、工具结果、预算压力等级和会话摘要候选标记；当前只做观测，不改变在线回复行为。
- [x] 已冻结客户会话摘要设计并完成独立数据层、生成 service、回复后异步触发、草稿保存、active 摘要只读注入和离线长上下文 smoke：`docs/architecture/customer-session-summary-design.md` 明确采用“观测触发、异步生成、下轮使用”，摘要只作为短期上下文，不写入长期客户画像，不引入 LangChain / LangGraph；当前已新增 `conversation_summaries` 表、model、repository、v014 migration、readiness / apply_migrations 必需表门禁、`conversation_summary_service.py` 摘要草稿生成器、`conversation_summary_scheduler.py` 回复后调度器、`conversation_summary_memory.py` 热路径只读 loader 和 `scripts/check_customer_long_context_summary_smoke.py` 离线冒烟脚本，正常 AI 回复保存和 `reply_latency` 记录后按 `context_budget.needs_session_summary_candidate` 排队生成 active 摘要，下一轮由 `chat_ai_loop` 只读加载并交给 `chat_context` 注入 system prompt；最近消息窗口、RAG 检索、客户画像、工具调用和长期画像写入规则不变，后续仍需生产观察真实 token 压力和回复质量。
- [x] 已冻结客户长期记忆治理计划：`docs/architecture/customer-memory-governance-plan.md` 明确长期画像只作为可审计服务提示，必须有来源证据、置信度、状态、撤销、过期和会话摘要隔离边界；`scripts/check_customer_memory_governance_plan.py --summary` 已接入统一业务合约门禁和生产预检业务合约明细。本片只补文档和静态验收，不改热路径、不改 `MemoryAgent` 写入策略、不改 `customer_profiles` 表结构。
- [x] 已冻结客户机器人可观测合约：`docs/architecture/customer-observability-contract.md` 明确知识命中率、无资料兜底率、转人工率、工具成功率、上下文压力率等指标，以及 `trace_id`、`channel_type`、`bot_type`、`intent`、`handoff_reason`、`fallback_reason` 等事件字段；`scripts/check_customer_observability_contract.py --summary` 已接入统一业务合约门禁和生产预检业务合约明细。本片只补文档和静态验收，不改客户机器人热路径、不新增数据库迁移、不引入 LangChain / LangGraph。
- [x] 已冻结 MiniApp 页面 API 覆盖合约：`docs/architecture/miniapp-page-api-coverage-contract.md` 明确首页、分类/商品、详情、购物车、结算、政策、地址、订单、客户群登记、客服和会员中心等页面依赖的 Platform API，并把会员权益、积分、储值余额、优惠券、配送费/满减/活动价列为待补 Platform API；`scripts/check_miniapp_page_api_contract.py --summary` 已接入统一业务合约门禁和生产预检业务合约明细。本片只补 Platform 侧契约和静态验收，不改 MiniApp 页面、不改 Platform API 行为。
- [x] 已冻结并实现知识库治理兼容迁移小切片：`docs/architecture/knowledge-governance-migration-plan.md` 明确继续复用 `knowledge_base`，当前已新增 `app/migrations/v015_knowledge_governance_fields.sql`、schema 默认字段、`KnowledgeEntry` 字段、`KnowledgeRepo` 默认发布治理过滤，以及 `/ready`、preflight、`apply_migrations.py` 字段级门禁；老数据默认 `audience=all`、`review_status=published` 保持兼容，`draft / archived / 过期知识` 不进入默认检索，指定 `audience=customer/employee` 时可同时返回 `all` 和目标 audience；同步新增 `scripts/check_knowledge_governance_plan.py`、测试，并接入统一质量门禁。
- [x] 已完成知识库入口 audience 分流：`KnowledgeRetriever` 支持固定 audience，客户机器人和有赞客服事件处理使用 `audience=customer`，企微员工助手订单/知识工具使用 `audience=employee`；默认 `knowledge_retriever` key 仍保持客户侧兼容，新增 `employee_knowledge_retriever` 供员工链路使用。
- [x] 已完成后台知识治理字段展示和编辑：知识配置 API、服务、仓库和 Vue 后台均已接入 `audience / review_status / valid_from / valid_until`，列表展示可见范围、发布状态和有效期，抽屉表单支持编辑；新增知识默认共同可见、已发布、长期有效。
- [x] 已补知识库 audience / 有效期治理 smoke：`scripts/check_knowledge_audience_governance_smoke.py --json` 使用内存库和真实 `KnowledgeRetriever` 证明默认视角只返回 `all`，客户视角返回 `all + customer`，员工视角返回 `all + employee`，草稿、归档、过期和未生效知识不会进入检索；后续仍需知识命中观测。
- [x] 已补首版知识命中日志、只读报表、后台只读 API 和后台只读页面：新增 `app/migrations/v016_knowledge_retrieval_logs.sql` 和 `knowledge_retrieval_logs` 表，`KnowledgeRetriever` 在客户/员工/no_match 检索后写入 bot_type、audience、query、retrieval_mode、命中知识 ID/标题、结果数和 fallback；`scripts/check_knowledge_retrieval_logs_smoke.py --json` 可用内存库证明客户命中、员工命中和 no_match fallback 均被记录；`scripts/report_knowledge_retrieval_logs.py --db <db> --limit 100 --json` 可只读输出命中数、no_match 率、机器人/audience/检索模式分布、按天趋势和最近日志；后台已新增 `/api/v1/admin/knowledge-retrieval-report/summary` 只读 API 和 `/knowledge-retrieval-report` 页面，展示命中率、按天 no_match 趋势、高频未命中、分布和最近日志。
- [x] 已补员工助手能力合约清单：`app/service/wecom/employee_agent_capability_contracts.py` 为 9 个员工助手能力补齐参数规则、缺参数口径、空结果口径、异常口径和探针名；`scripts/check_employee_agent_capability_contracts.py --json` 会检查能力卡、合约和探针一致，当前 66 项通过。本片只补治理和验证资产，不改变员工助手 planner、工具调用或确定性回复。
- [x] 已补 GitHub 参考实施计划静态门禁：`scripts/check_github_reference_implementation_plan.py --summary` 会校验主计划阶段状态、执行边界、已落地资产、LangGraph 试点限制和禁止性指令，防止后续把“参考借鉴”误改成全量 LangChain / LangGraph 迁移。本片只补文档和静态验收，不改客户热路径、不改员工助手主链路、不新增依赖。
- [x] 业务合约已接入统一质量门禁：`scripts/check_project.py --skip-tests` 新增“业务合约检查”，会运行员工助手能力合约、客户 RAG golden cases、知识治理计划、客户长期记忆治理计划、客户机器人可观测合约、MiniApp 页面 API 覆盖合约、GitHub 参考实施计划七类 `--summary` 静态检查；后续新增员工助手工具、删改客服核心评测场景、调整知识治理边界、改长期记忆边界、改客户机器人观测边界、改 MiniApp 页面/API 边界或改 GitHub 参考实施计划边界时，不能绕过统一门禁。
- [x] 业务合约已接入生产预检：`scripts/preflight_production.py --json` 新增 `business_contracts.static_checks`，发布前会报告员工助手能力合约、客户 RAG golden cases、知识治理计划、客户长期记忆治理计划、客户机器人可观测合约、MiniApp 页面 API 覆盖合约、GitHub 参考实施计划七类状态明细；合约失败时 recovery plan 只提示运行统一质量门禁和修复对应合约，不自动改库或改配置。
- [x] 已补预检报告业务合约证据复核脚本：`scripts/check_preflight_business_contracts.py <preflight-report.json> --summary` 可只读校验归档 preflight JSON 中 `business_contracts.static_checks` 存在、通过，并列出七类业务合约均为 passed；用于发布证据归档后的二次确认。
- [x] 已补 Harness 证据索引结构门禁：`scripts/check_evidence_index.py --summary` 会只读校验 `docs/harness-engineering/core/evidence-index.md` 的必填字段、结果枚举、敏感数据标记、重复 ID 和预检业务合约证据引用，并已接入 pre-commit `check-evidence-index`。

______________________________________________________________________

## 2026-07-04 企微员工助手 Agent 生产化切片
- [x] 员工助手回复期 LLM 润色已移除并已同步生产 `0.74.32`：员工可见回复链路改为 `query -> planner -> tools -> deterministic reply -> clean_plain_text_reply -> return`，`EmployeeAgentPlanner` 的 LLM 仅保留在结构化规划兜底阶段，不再生成员工可见文本。已删除 `employee_agent_reply_guard.py` 和 `employee_agent_order_list_guard.py` 两个旧润色回退守卫，测试从验证“润色后回退”改为验证“确定性直出保留库存、未命中保护语、订单列表行级结构和 Markdown 清理”。本地员工助手相关 91 条测试通过，45/45 规划探针通过，全量 `python -m pytest tests/ -q` 通过且覆盖率 79.08%；生产 `/health` 返回 `status=ok,version=0.74.32`，`/ready` 返回 `status=ready,version=0.74.32`，45/45 加密回调探针报告 `reports/wecom-employee-agent/callback-20260705-151936.json` 通过。
- [x] 商品无库存和未命中回复口径本地收口，生产同步待执行：生产抽查发现 `招牌牛奶吐司还有吗` 命中 0 库存商品后，LLM 润色可能编造具体替代品名；`不存在的月球蛋糕还有吗` 未命中商品后，回复可能丢掉“未命中不等于缺货”的保护语。已在 `employee_agent_reply_guard.py` 增加商品未命中保护语守卫和无库存替代品幻觉守卫；确定性结果包含“未命中结果 / 缺货结论”时，润色必须保留“未命中”和“缺货”关系；确定性结果只提示“同品类或相近价位替代款”时，润色不能编造具体替代品名或示例。首次生产复查暴露“如北海道吐司 / 原味手撕包”仍会漏过，已把“如 + 具体替代品”纳入同一守卫，并把 `北海道吐司`、`原味手撕包` 加入共享探针禁用词。共享探针新增 `no-stock-product` 和 `missing-product`，规划与真实回调验收从 43 条扩展到 45 条。聚焦回归 9 条通过，补漏聚焦回归 3 条通过，员工助手和企微插件相关测试通过，规划探针 45/45 通过，Ruff、文件体量、项目红线、架构扫描、编码检查、mistake ledger 和 diff 空白检查均已通过；实际版本已升到 `0.74.30`，等待最终生产同步后补 `/health`、`/ready`、45/45 回调探针和真实回复抽查证据。
- [x] 商品高库存下一步动作已同步生产：商品查询工具的下一步动作已从固定“低库存商品建议尽快确认”改为按命中商品库存动态生成。未命中会提示换商品名/品类/关键词且不要把未命中当缺货；无库存会提示不要承诺有货并推荐替代款；低库存会提示确认数量、规格和配送/自提时间；高库存会提示可按当前库存回复，特殊数量再人工核对。43 问探针中当前明确要求 `库存 72` 的高库存商品问法已禁止出现“低库存”。本地聚焦回归 4 条通过，员工助手和企微插件相关测试通过，43 问规划探针 43/43 通过，Ruff、文件体量、项目红线、架构扫描、编码检查、mistake ledger 和 diff 空白检查均已通过；已同步生产 `0.74.27 / 786b738a3`，`/health` ok，`/ready` ready，线上 43/43 端到端加密回调探针通过；商品完整回复抽查 `casual-inventory`、`casual-product-stock`、`order-product-inventory`、`product-stock-customer-reply` 均保留 `库存72`，且未出现“低库存”误导提示。
- [x] 普通订单列表结构保真已同步生产：针对 `pending-shipment-list`、`casual-pending-shipment`、`missing-logistics-list`、`casual-missing-logistics`、`weekend-pending-orders` 等普通订单列表继续收紧员工可读性。确定性结果中多条订单行如果包含尾号、状态、金额和物流标记，LLM 润色必须保留相同数量级的行级字段；否则回退确定性工具结果。通用列表守卫已拆到 `employee_agent_order_list_guard.py`，避免回复守卫主文件继续膨胀；物流计数使用长词优先的非重叠匹配，避免把 `暂无物流` 同时算作 `无物流`。43 问探针已要求待发货和无物流列表包含尾号、物流和状态词。本地新增聚焦回归 5 条通过，员工助手相关测试通过，43 问规划探针 43/43 通过，Ruff、文件体量、项目红线、架构扫描、编码检查、mistake ledger 和 diff 空白检查均已通过；已同步生产 `0.74.26 / 8b669d8e8`，`/health` ok，`/ready` ready，线上 43/43 端到端加密回调探针通过；普通订单列表解密抽查 `pending-shipment-list`、`casual-pending-shipment`、`missing-logistics-list`、`casual-missing-logistics`、`tomorrow-pending-orders`、`weekend-pending-orders` 均保留“尾号 / 待发货或待收货 / 金额 / 暂无物流”。
- [x] 履约风险订单列表结构保真已同步生产：针对 `fulfillment-risk-list` 和 `casual-fulfillment-pressure` 继续收紧员工可读性，履约风险列表标题已明确“按约送时间从早到晚展示”，下一步动作提示先处理已过约送时间或暂无物流订单；LLM 润色若把多单列表压缩成摘要，导致缺少尾号、约送、物流、待发货/待收货状态，或减少尾号数量，会回退确定性工具结果。43 问探针已要求履约风险和发货压力类回复同时包含“尾号 / 约送 / 物流”。本地新增聚焦回归 3 条通过，员工助手相关测试通过，43 问规划探针 43/43 通过，Ruff、文件体量、项目红线、架构扫描、编码检查、mistake ledger 和 diff 空白检查均已通过；已同步生产 `0.74.24 / 121b1331a`，`/health` ok，`/ready` ready，线上 43/43 端到端加密回调探针通过；履约风险完整回复抽查 `fulfillment-risk-list`、`casual-fulfillment-pressure`、`today-action-items` 均保留“尾号 / 约送 / 物流”，且履约风险列表按约送时间展示。
- [x] 已过约送时间履约风险单标记已同步生产：针对 `fulfillment-risk-list` 和 `casual-fulfillment-pressure` 继续收紧员工可读性，订单配送时间若早于当前北京时间，会在确定性订单行中追加“已过约送时间”；LLM 润色若未保留已过/逾期/超时语义，或把已过约送时间误写成“需在 / 前完成 / 前安排”等未来截止表达，会回退确定性工具结果。43 问探针已把履约风险和发货压力类回复加入同类禁用词。本地新增聚焦回归 3 条通过，员工助手相关测试 82 条通过，43 问规划探针 43/43 通过，Ruff、文件体量、项目红线、架构扫描、编码检查、mistake ledger 和 diff 空白检查均已通过；已同步生产 `0.74.23 / cae499c82`，`/health` ok，`/ready` ready，线上 43/43 端到端加密回调探针通过；履约风险完整回复抽查 `fulfillment-risk-list`、`casual-fulfillment-pressure`、`today-action-items` 均保留“已过约送时间 / 已过”语义，且未出现“需在 / 前完成 / 前安排”。
- [x] 履约风险约送日期和销量排行备货口径守卫已同步生产：针对 `fulfillment-risk-list` 和 `casual-fulfillment-pressure` 这类问法，已在回复事实保真层拦截 LLM 把工具结果中的 `约送 YYYY-MM-DD` 绝对日期改写成“明天 / 后天 / 周末 / 下周”等相对日期口径；一旦润色新增工具结果里没有的相对日期，就回退确定性工具结果，避免把已过期履约风险误说成未来风险。同步 `0.74.20 / 6bc3ec5a5` 后线上 43 问首次验收暴露 `this-week-top-products` 被润色成“优先备货”，已继续把销量排行“优先备货”守卫从并列场景扩展到所有销量排行工具结果，避免只凭销量排行给备货动作。43 问探针已把履约风险和发货压力类回复加入相对日期禁用词，销量排行类继续禁止“优先备货”。本地聚焦回归、员工助手相关测试、43 问规划探针、Ruff、文件体量、项目红线、架构扫描、编码检查、mistake ledger 和 diff 空白检查均已通过；已重新同步生产 `0.74.21 / 3f80aa025`，`/health` ok，`/ready` ready，线上 43/43 端到端加密回调探针通过，`this-week-top-products` 预览已不再出现“优先备货”，履约风险类预览未再出现错误相对日期漂移。
- [x] 无物流订单关闭/退款口径守卫已同步生产：针对 `casual-missing-logistics` 这类“哪些单子还没出物流”问法，已在回复事实保真层拦截 LLM 润色凭空声明“已剔除 / 不含已关闭 / 不含退款 / 剔除已关闭 / 剔除退款”；确定性结果若包含“暂无物流 / 无物流”但没有明确同样排除口径，润色一律回退到工具结果，避免员工误判列表范围。43 问探针已把无物流列表类回复加入同类禁用词。本地聚焦回归、员工助手相关测试、43 问规划探针、Ruff、文件体量、项目红线、架构扫描、编码检查、mistake ledger 和 diff 空白检查均已通过；已同步生产 `0.74.19 / 3adede196`，`/health` ok，`/ready` ready，线上 43/43 端到端加密回调探针通过，`missing-logistics-list` 和 `casual-missing-logistics` 预览均未出现“已剔除 / 不含已关闭 / 不含退款”。
- [x] 销量并列时的爆款判断已同步生产：针对 `casual-top-product` 这类“今天卖爆的是哪个”问法，已在销量排行工具结果中识别第一名并列，低样本并列提示“还不能判断单一爆款”，高样本并列提示结合金额、库存和后续订单判断主推商品；LLM 润色若把并列结果改写为“销量第一 / 当前爆款 / 优先备货”会回退确定性结果；43 问探针已禁止销量排行类回复只凭排行给出“优先备货”。同时把销量排行格式化拆到独立模块，避免订单格式文件超出体量门禁。本地聚焦回归、员工助手相关测试、43 问规划探针、Ruff、文件体量、项目红线、架构扫描、编码检查、mistake ledger 和 diff 空白检查均已通过；已同步生产 `0.74.18 / 4c38fadcb`，`/health` ok，`/ready` ready，线上 43/43 端到端加密回调探针通过，`casual-top-product` 预览已不再出现“优先备货”。
- [x] 员工助手 Markdown 引用符清理已同步生产：生产预览发现 `refund-order-customer-reply` 可能出现 `>` blockquote 引用符；已在统一纯文本清理函数中清除行首 `>`，并把 callback 语义验收升级为拒绝 `>` 引用符。聚焦回归、员工助手相关测试、43 问规划探针、Ruff、文件体量、项目红线、架构扫描、编码检查、mistake ledger 和 diff 空白检查均已通过；已同步生产 `0.74.17 / d562e5d0d`，`/health` ok，`/ready` ready，线上 43/43 端到端加密回调探针通过，`refund-order-customer-reply` 预览已无 `>` 引用符。
- [x] 订单+客户话术混合问法已同步生产：`pending-shipment-customer-reply` 和 `refund-order-customer-reply` 不再只依赖订单列表或 LLM 自由润色；`order_dynamic_query + knowledge_answer` 混合结果会在订单工具结果后追加“给客户可复制回复”，退款/售后与未发货场景分开生成确定性话术，空订单结果也给保守回复；回复守卫要求 LLM 润色保留“客户 / 回复”语义，否则回退确定性结果。43 问探针已把这两类样本升级为必须同时包含“客户 / 回复”，本地相关测试、规划探针、红线和架构扫描通过；已同步生产 `0.74.16 / 712ec0533`，`/health` ok，`/ready` ready，线上 43/43 端到端加密回调探针通过。
- [x] 客户线索和客户群空结果可读性已同步生产：`customer-lookup` 空结果不再返回“未找到匹配客户地址”，改为按员工原查询提示“没找到某客户的客户地址线索”，并建议换客户姓名或地址关键词再查；客户线索查询会先清理“查一下 / 地址线索 / 地址”等口语噪声，把“查一下张三地址线索”归一为 `张三` 再查，手机号等敏感查询只在工具 payload 和员工回复中展示脱敏预览；`group-campaign-summary` 不存在批次不再作为系统失败提示“活动批次不存在 / 请稍后重试”，改为确定性未命中，提示确认 `campaignId` 是否复制完整或到后台客户群活动列表按群名/标题查批次。43 问探针已加入旧空结果文案禁用词，本地相关测试、员工助手测试、规划探针、红线和架构扫描通过；已同步生产 `0.74.15 / 9addc9fc5`，`/health` ok，`/ready` ready，线上 43/43 端到端加密回调探针通过，`customer-lookup` 预览已只展示 `张三` 线索结果，不再回显整句查询。
- [x] 离线复盘摘要员工可读性已同步生产：`offline-review-summary` 的员工可见 `result` 和 `nextAction` 已不再暴露 `outside_night_window`、`skippedReason` 等内部调度字段；真实跳过原因会转成“当前不在夜间复盘窗口，最近一轮没有执行”，未知原因转成后台日志确认提示；结构化 `skippedReason` 字段仍保留给单工具调试。43 问探针已把裸内部字段加入离线复盘禁用词，本地规划探针 43/43、员工助手 service/API/callback 相关测试通过；已同步生产 `0.74.13 / e27090cb1`，`/health` ok，`/ready` ready，线上 43/43 端到端加密回调探针通过，离线复盘生产预览已为中文员工话术。

______________________________________________________________________

## 2026-06-11 生产同步前本地补强记录
- [x] 前台会话渠道常量收口：新增 `app/constants/storefront.py`，`StorefrontConversationService` 已改为依赖 storefront 语义常量；兼容期内继续复用既有微信小程序 channel、demo 用户、`miniapp:` 消息 ID 前缀和默认转人工原因，确保外部 API 与历史会话行为不变。
- [x] Platform 架构收口 P4：后端 API 真实实现已统一迁入 `app/api/admin/`、`app/api/channels/storefront/`、`app/api/integrations/`；旧根目录 API 文件只保留兼容入口；`lifespan` 路由装配优先导入 canonical router，`scripts/check_project.py` 已加入 `根 API 兼容文件仅作为兼容入口` 红线。
- [x] P4 后双仓联动预检：MiniApp `release:readiness` 已从 19/22 修复到 21/22，backend transfer 测试路径和 secret hygiene 误报已收口；唯一剩余失败是生产商品分类数据质量，不是后端 API 目录收口回归。
- [x] 小程序商品泛化分类兜底：`商品`、`价格`、`推荐`、`在售` 等有赞同步噪声标签不再作为 `/api/v1/miniapp/products` 的 `categoryId/categoryName` 返回；无法解析公开分类时统一落到 `youzan-products / 有赞同步商品`，并已补 service/API 回归测试。
- [x] 后台生产入口恢复：修复 `web/admin/src/services/assets.ts` 的默认导入问题，使后台可重新执行 `npm run build:production`；并修正 `app/api/admin/frontend.py` 在 API 目录迁移后的项目根计算，确保 `/admin/` 指向项目根下的 `web/admin/dist/index.html`。
- [x] Platform 架构收口 P1-P3：`order` 域、`channels/storefront/auth.py` 和 MiniApp API 内部默认用户已依赖 storefront 常量；服务测试文件名与商品 helper 已切到 canonical 语义；`miniapp` service 文件继续作为薄兼容 facade，不承载真实逻辑。
- [x] `lifespan` 兼容 key 降噪：`app/lifespan_services.py` 和 `app/main.py` 已通过 `LEGACY_SERVICE_ALIASES`、`LEGACY_REPOSITORY_ALIASES` 集中补旧 `miniapp_*` key；真实装配先声明 canonical key，测试也补充旧 key 指向 canonical 对象的断言。
- [x] 地址域 canonical repo/model 第一批：新增 `CustomerAddress`、`CustomerAddressAuditEntry`、`CustomerAddressRepo`、`CustomerAddressAuditRepo`，`customer` 服务和地址测试已优先依赖 customer 语义命名；旧 `MiniappAddress*` 与 `miniapp_address_*` 模块保留为兼容导出，数据库表名和 `/api/v1/miniapp/addresses` 路径不变。
- [x] 测试依赖迁移第一批：订单、支付和会话 API 相关测试已从 `MiniappPaymentService`、`MiniappOrderInventoryService`、`app.service.miniapp_payment` monkeypatch 路径迁到 `OrderPaymentRuntimeService`、`OrderInventoryService`、`order.payment_state` 与 `integrations.wechat_pay`；搜索结果仅剩红线测试样例和 `app/service/miniapp_*.py` 兼容 facade。
- [x] Platform 领域迁移盘点：新增 `docs/architecture/platform-domain-migration-inventory.md`，确认 `app/service/miniapp_*.py` 已基本退为兼容 facade，真实实现主要落在 `customer / order / catalog / conversation / channels/storefront / integrations / ops`；下一阶段优先迁测试和内部依赖，地址域暂以 repo/model 别名过渡，不改 `/api/v1/miniapp/*`、`x-miniapp-user-id` 或 `miniapp_addresses` 表名。
- [x] README 目录树模型路径修正：已将不存在的 `app/models/youzan_product.py` 从目录树示意中移除，改为当前真实存在的 `app/models/order.py`。
- [x] quick reference 路径修正：`docs/AGENTS/quick-reference.md` 中“数据库初始化”入口已从不存在的 `app/repository/database.py` 修正为当前真实存在的 `app/database.py`。
- [x] README 失效脚本入口清理：快速开始、安装配置、运维命令和 FAQ 中已不再引用 `init_db.py`、`seed_knowledge.py`、`sync_youzan_products.py` 这类不存在的旧脚本，而是统一改成当前真实存在的 `apply_migrations.py`、`seed_baseline_knowledge.py` 和 `sync_real_products_from_youzan.py`。
- [x] README 仓库占位链接清理：快速开始、安装部署和生产部署示例中的主仓 clone 地址已切换为真实仓库链接；fork 场景保留 `your-github-name` 变量，但 `upstream` 已明确指向真实主仓。
- [x] README 高可见仓库名示例收口：目录树根节点已改成 `Platform (repo: YunxiBakeBot)/`，底部联系方式已切到真实仓库地址，UTF-8 控制台脚本注释也已改成 repo 语义，进一步减少仓库名被误读成产品名的概率。
- [x] 当前权威文档的代码仓路径语义收口：`customer master v1`、API 契约、有赞客户迁移文档和 Harness 父入口中的 `YunxiBakeBot` / `YunxiBakeMiniApp` 提法已进一步收紧为“代码仓路径”语义，避免把仓库名误读为产品角色名。
- [x] 平台可见展示名收口：README 的 systemd `Description` 以及 `scripts/apply_migrations.py`、`scripts/preflight_production.py`、`scripts/rebuild_embeddings.py`、`scripts/seed_baseline_knowledge.py` 的命令说明/输出标题已切换为 `Platform` 通用口径，避免把 `YunxiBakeBot` 继续误读为产品展示名；仓库路径、目录名和 `yunxibakebot` 服务标识保持不变。
- [x] 正式有赞客户迁移后核对：新增 `scripts/verify_youzan_customer_import.py` 与 `tests/scripts/test_verify_youzan_customer_import.py`，支持按 `source_batch_id` 核对快照数、主档数、来源身份数、手机号身份数、复核单数和 bucket summary，并可选对比 apply 报告，作为正式迁移后的批次级自动验收。
- [x] 正式有赞客户迁移 runbook：新增 `docs/architecture/youzan-customer-formal-import-runbook.md`，把审计入口、正式入口、批次号约定、报告命名、同批次重跑语义、何时允许 `--allow-create`、迁移后最小验证收成标准执行说明，并同步接入 `docs/README.md` 与 Harness `verification-matrix`。
- [x] 正式有赞客户迁移交接与回滚 runbook：新增 `docs/architecture/youzan-customer-import-handoff-and-rollback-runbook.md`，补齐 apply 之后的异常中止点、交接证据、同批次重跑、必须换批次和不能直接回滚的判断路径，并同步接入 `docs/README.md`。
- [x] 正式有赞客户迁移入口：新增 `scripts/import_youzan_customers.py` 与 `tests/scripts/test_import_youzan_customers.py`，把“审计”和“正式迁移”职责拆开；新脚本默认 dry-run，不写库也能输出 planned bucket summary、数据库可用性与 schema 状态，显式 `--apply` 时复用现有 customer 导入链路执行写库，并支持 `--allow-create`、`--json --output` 留档。
- [x] customer 试导入闭环：新增 `app/service/customer/importer.py`，并扩展 `customer_master` 模型、仓库与服务，使 `customer_source_snapshots`、`customer_merge_reviews` 不再是空表；`scripts/audit_youzan_customer_migration.py` 已支持 `--apply-import` 试导入模式，并完成一次真实 CSV 的内存导入验证，当前分流结果为 `auto_merge=13551`、`new_master=11175`、`pending_review=0`。
- [x] customer 重跑幂等收口：customer 试导入现已显式区分 `youzan_customer(source_record_id)` 来源身份与 `phone` 归并身份；同批次重跑会直接复用既有 `customer_source_snapshots` 并返回 `skip_existing_batch_row`，跨批次会复用既有来源身份，`pending_review` 也会复用同一条复核记录并追加证据快照；脚本层文件库双跑测试已覆盖该行为。
- [x] customer 主档最小代码骨架：新增 `app/models/customer_master.py`、`app/repository/customer_master_repo.py`、`app/service/customer/master.py` 以及对应测试，先打通主档创建、按手机号查询、身份挂接和缺失客户保护；说明 customer 域已经不再停留在 schema 层，后续可以直接往试导入编排推进。
- [x] `customer master v1` 四表迁移基线：已将 `customer_master`、`customer_identity_links`、`customer_source_snapshots`、`customer_merge_reviews` 正式加入 `app/migrations/schema.py`，并同步更新 `REQUIRED_DATABASE_TABLES` 与迁移测试；说明 customer 域已经完成从“schema 草案”到“DDL 基线落地”的推进，后续可以直接进入 repository / service 和试导入脚本实现。
- [x] `customer master v1` 四表 schema 草案：新增 `docs/architecture/customer-master-v1-schema-draft.md`，明确 `customer_master`、`customer_identity_links`、`customer_source_snapshots`、`customer_merge_reviews` 的字段、主外键、唯一约束、索引、枚举状态和 `pending_review` 闭环；下一步可据此进入 `app/migrations/schema.py` 的 DDL 落地与 `customer` 域 repository 骨架实现。
- [x] 有赞客户迁移审计脚本：新增 `scripts/audit_youzan_customer_migration.py` 与 `tests/scripts/test_audit_youzan_customer_migration.py`，可直接读取本地有赞客户 CSV 与订单 CSV，输出汇总 JSON、汇总指标表、客户问题表、客户分流表；首轮真实审计结果显示当前手机号重复冲突极低，但无有效手机号客户仍有 `11175`，后续 `customer master v1` 需要重点承接弱身份主档与企微/订单补强路径。
- [x] 小程序商城主链路：新增 `/api/v1/miniapp/auth/login`、`/chat/messages`、`/addresses`、`/orders`、`/payments/wechat/notify`，支持 demo 登录兜底、客服消息、地址簿、订单创建/取消/支付准备与支付回调入口，库存按订单占用/释放，订单事件可供用户侧时间线展示。
- [x] 后台经营台工作流：新增后台装修、订单、顾客地址、店铺配置与经营概览页面，对应后端开放装修配置、装修素材上传、订单看板与地址管理 API，并补充移动端底部导航、结构检查脚本与浏览器 smoke 脚本。
- [x] 小程序商品分类稳定字段链路：新增 `/api/v1/miniapp/product-categories` 与 `/api/v1/miniapp/products?categoryId=youzan-classification-*`；有赞 `ITEM_INFO` 返回的 `classification_id`、`leaf_category_id` 标准化落库为 JSON 数组字段；分类中文名由 `youzan.item.classification.search/1.0.0` 的 `classification_id -> name` 映射提供，避免继续把商品名、规格、价格或推荐标签误当分类。
- [x] ITEM_INFO 批量同步批次修正：生产联调确认 `youzan.item.base.search/1.0.0` 每次稳定只返回 10 个 `item_id`，已将对账批次从 20 收紧为 10，并新增回归测试覆盖“上游最多返回 10 条”的漏数场景。
- [x] ITEM_INFO 回填范围修正：生产联调确认历史下架商品 `item_id` 混入 ITEM_INFO 批次时，可能让整批接口直接回空；现已将 ITEM_INFO 分类回填范围收敛到当前有赞在售商品，避免历史宽表数据污染小程序分类同步。
- [x] AI 降级自动转人工：LLM API 异常、响应解析异常、工具轮次耗尽会记录结构化失败原因，并自动创建转人工工单，避免客户停在“系统忙”无人接管状态。
- [x] 生产级补强对比流程图：新增 [docs/production-readiness-before-after.html](docs/production-readiness-before-after.html)，用双栏流程图展示修改前的上线风险与修改后的预检、修复、留档闭环；已补充客户体验、图片理解、就绪门禁、数据库、知识向量、留档审计六块细节矩阵，以及明日同步生产泳道、当前本地运行时缺口和验收信号；README 已增加入口链接，便于明日同步生产前快速讲清变更价值。
- [x] Vibe Coding Harness Engineering：新增 [docs/harness-engineering/README.md](docs/harness-engineering/README.md) 作为统一父入口，并在 `core/`、`adr/`、`specs/` 下管理追溯模型、验证矩阵、防重犯账本、Agent 交接模板、证据索引、ADR 和生产级设计，用于把需求、决策、验证、证据、复盘和后续防线串成可追溯闭环。
- [x] Harness 信息架构对比图：新增 [docs/harness-engineering/before-after.html](docs/harness-engineering/before-after.html)，用可离线打开的双栏页面展示调整前的散点式入口，以及调整后的 `docs/harness-engineering/` 父目录、`core/`、`adr/`、`specs/` 三层收纳方式。
- [x] Harness Skill 统一接入：新增 `.agents/skills/yunxi-harness-engineering/SKILL.md`，作为较大任务、追溯、复盘、防重犯、证据留档、交接和 Skill 审计的项目级入口；`AGENTS.md`、`docs/AGENTS/skill-reference.md` 和 4 个项目 Guard Skill 已联动到该入口，避免 Skill 与 Harness 文档各管一摊。
- [x] Harness P1 机器辅助：新增 `python scripts/harness_snapshot.py`，可生成 Markdown/JSON 交接快照，自动带 trace、目标、最新 LOGBOOK、工作区状态和参考入口；新增 `python scripts/check_mistake_ledger.py` 并接入 pre-commit `check-mistake-ledger` hook，检查防重犯账本空账本标记、条目标题、必填字段和枚举合法性，避免 mistake ledger 变成不可读流水账；新增 `python scripts/check_evidence_index.py` 并接入 pre-commit `check-evidence-index` hook，检查证据索引必填字段、重复 ID 和关键证据引用。
- [x] 中文编码与终端乱码治理：新增 [docs/AGENTS/encoding-and-terminal.md](docs/AGENTS/encoding-and-terminal.md)、`.editorconfig`、`scripts/enable_utf8_console.ps1` 和 `scripts/check_text_encoding.py`；当前用户 PowerShell profile 已加入带标记的 UTF-8 bootstrap，使新终端默认按 UTF-8 读写中文文本；pre-commit 新增 `check-text-encoding`，防止中文文档、Skill 和 Harness 入口混入真实 mojibake。
- [x] Harness 决策与证据索引：新增 [docs/harness-engineering/adr/README.md](docs/harness-engineering/adr/README.md) 和 [ADR 0001](docs/harness-engineering/adr/0001-traceable-memory-harness.md)，固化采用 Traceable Memory Harness 的长期决策；新增 [docs/harness-engineering/core/evidence-index.md](docs/harness-engineering/core/evidence-index.md) 与 `reports/harness/.gitkeep`，用于登记交接、预检、冒烟、迁移、知识种子、向量重建等证据包。
- [x] 转人工接手摘要增强：接手提示优先走 LLM 总结，固定输出“客户诉求 / 当前卡点 / 建议接手”，失败或超时回退确定性摘要。
- [x] 图片理解提示增强：图片消息进入多模态模型前追加观察任务，要求提取款式、文字、数量、颜色、尺寸、异常和待确认项。
- [x] 上线值守摘要：新增观察台摘要聚合与后台可视化卡片，覆盖回写失败、Webhook 失败、慢 Webhook、处理中队列。
- [x] 冒烟脚本扩展：`scripts/smoke_test.py` 已校验 `.env`、数据库、15 张生产关键表结构、知识行、向量索引、关键配置、运行开关、生产通道、后台 dist、`/health`、`/ready`、观察台摘要接口。
- [x] 冒烟结果留档：`scripts/smoke_test.py --json` 输出机器可读报告，包含生成时间、项目根、实际数据库路径、向量路径、服务地址、逐项结果和失败项名称；HTTP 异常会显示异常类型，便于同步生产后归档和排障。
- [x] 冒烟恢复提示：`scripts/smoke_test.py` 会把失败项归并为 `recovery_hints`，覆盖数据库/知识、向量缓存、生产配置、后台产物和服务可达性；每条提示带稳定 `key` 与 `severity`，便于部署脚本或值守归档筛选；服务端口不可达时会明确说明后续 HTTP 接口失败属于同一根因，避免把跳过项误判成多条独立故障。
- [x] 安全写入留档文件：`scripts/preflight_production.py --json --output <file>` 与 `scripts/smoke_test.py --json --output <file>` 可直接写入 Windows 友好的 UTF-8 BOM JSON 报告；若目标文件已存在会返回错误并拒绝覆盖，避免上线前后报告互相覆盖。
- [x] 冒烟脚本稳态兜底：数据库与向量路径复用运行时统一解析；若 `knowledge_base` 尚未迁移，冒烟脚本返回明确失败报告，不再因 SQLite 异常中断。
- [x] `/ready` 就绪门禁：返回非敏感布尔检查和运行开关；当前覆盖 Admin Token、MiMo Key、数据库路径、关键表结构、向量索引缓存文件、有赞生产模式、企微配置、人工接手人、后台静态产物。
- [x] 运行路径口径统一：实际数据库连接、向量缓存读写、`/ready` 中 `DB_PATH` 与 `EMBEDDING_INDEX_DIR` 的相对路径统一按项目根目录解析，避免生产 systemd 工作目录变化导致应用和预检看向不同文件。
- [x] 后台 dist 防漏构建：后台产物必须包含 `/observability/summary`、上线值守、慢 Webhook 标记，否则 `/ready` 和 smoke 会暴露降级/失败。
- [x] 生产通道配置门禁：有赞生产冒烟拒绝 `YOUZAN_MOCK_MODE=True`；企微要求 `WECOM_CORP_ID`、`WECOM_AGENT_ID`、`WECOM_SECRET`、`WECOM_TOKEN`、`WECOM_ENCODING_AES_KEY`、`WECOM_KF_ID`，人工接手要求 `WECOM_STAFF_ID` 或 `WECOM_KF_SERVICER_USERID`。
- [x] 生产预检报告：新增 `python scripts/preflight_production.py`，只读汇总 `/ready` 门禁、缺失关键表、知识库有效行、向量索引缓存文件和可执行修复动作；支持 `--json` 输出机器可读报告，并支持 `--db-path` / `--index-path` 对生产快照或临时路径做预检。
- [x] 预检留档元信息：`scripts/preflight_production.py --json` 顶层输出 `metadata`，包含生成时间、项目根、实际数据库路径、实际向量索引路径、最终 smoke 服务根地址和应用版本，便于明日上线前后报告归档与复盘。
- [x] 上线修复计划：`scripts/preflight_production.py` 会把失败项自动归并成 `recovery_plan`，按“配置确认 → 数据库迁移 → 知识导入 → 向量重建 → 后台构建（如 dist 缺失或过旧）→ 最终验证”的顺序输出 dry-run 与显式 `--apply` 命令；每个计划步骤在 JSON 中带稳定 `key`、`severity` 与 `apply_mutates_state`，文本报告显示 `[severity] key` 和 `apply(mutates_state=yes/no)`，便于明日区分写入动作、纯验证动作并接入部署脚本；最终验证步骤同时保留带 `--base-url`、`--db-path`、`--index-path` 的文本冒烟命令，以及 `python scripts/smoke_test.py --json --base-url <服务根地址> --db-path <目标库路径> --index-path <向量索引基路径> --output reports/smoke-after-{timestamp}.json` 留档命令，脚本会自动把 `{timestamp}` 展开为当前时间；同步生产后可给 preflight 追加 `--smoke-base-url <实际生产域名>`，让 `recovery_plan` 直接输出生产域名版冒烟命令。`--smoke-base-url` 只接受 `http(s)://host[:port]` 根地址，不能带 `/ready` 等路径、参数或查询串。
- [x] 防误写口径统一：数据库、知识库和向量缓存失败项的单条 action 统一提示先查看 `recovery_plan` 或运行 dry-run，确认目标库/目标路径后再显式 `--apply`；`apply_migrations.py`、`seed_baseline_knowledge.py` 和 `rebuild_embeddings.py` 自身也要求先确认 dry-run 输出，缺迁移时先回到迁移 dry-run，避免明日按单项修复时绕过检查。
- [x] 坏数据库文件防误修：`preflight_production.py` 与 `apply_migrations.py` 会识别目标文件存在但不是可读 SQLite 的情况，提示先核对 `DB_PATH` 或恢复数据库文件，不再把坏文件当作普通缺表库继续生成迁移、知识导入或向量重建 apply 路径。
- [x] 坏数据库单项检查防误导：当 DB 文件不可读时，知识库有效数据检查会提示先修复数据库文件，不再提示执行基础知识 seed 写入。
- [x] 冒烟坏数据库提示：`smoke_test.py` 在数据库文件不可读时会报告 `database_not_readable` 并提示核对 `DB_PATH` 或恢复数据库文件，避免上线后把坏文件误判为普通缺表或知识查询失败。
- [x] 显式数据库迁移命令：新增 `python scripts/apply_migrations.py`，默认 dry-run 只列缺失表；需要写库时显式加 `--apply`，用于明日同步生产前补齐关键表结构；支持 `--json --output reports/migration-{timestamp}.json` 留档，写入 JSON 文件时带 UTF-8 BOM、自动展开 `{timestamp}` 且拒绝覆盖已有报告。若目标库不存在，`--apply` 默认拒绝创建新库，必须确认路径无误后才加 `--allow-create`；若 dry-run 显示 `database schema already ready`，无需重复执行 `--apply`。
- [x] 基础客服知识种子：新增 `python scripts/seed_baseline_knowledge.py`，默认 dry-run 不写库；确认目标库无误后加 `--apply` 写入保守兜底知识，覆盖下单信息收集、价格库存口径、定制咨询、配送取货、售后和转人工边界。支持 `--json --output reports/baseline-seed-{timestamp}.json` 留档，JSON 顶层包含 `status`、`metadata` 和 `report`，写入文件带 UTF-8 BOM、自动展开 `{timestamp}` 且拒绝覆盖已有报告。若 7 条基础知识已存在，dry-run 会以成功退出并提示无需重复 `--apply`；若目标文件不是可读 SQLite，会返回明确失败报告而非 traceback。
- [x] 显式向量重建命令：新增 `python scripts/rebuild_embeddings.py`，默认 dry-run 只检查数据库结构、有效知识条数和目标缓存文件；知识库导入后显式加 `--apply` 生成 `data/embeddings.npy` 与 `data/embeddings.json`。若 dry-run 显示 `schema_ready=False`，先执行数据库迁移；若显示 `embedding cache already ready`，无需重复执行 `--apply`。
- [x] 入口文件体量治理：`/ready` 判定已拆到 `app/readiness.py`，避免 `app/main.py` 超过文件体量门禁。
- [x] 配置加载路径固定：`app/config.py` 默认按项目根目录读取 `.env` 和 `VERSION`，避免生产服务从非项目根工作目录启动时误读或漏读配置。

### 明日同步生产前必须确认
- [ ] 同步前先运行 `python scripts/preflight_production.py`，按 `recovery_plan` 顺序补齐失败项；需要留档或接入部署脚本时运行 `python scripts/preflight_production.py --json --output "reports/preflight-before-{timestamp}.json"`，报告顶层 `metadata` 会记录检查时间、实际数据库路径、向量路径、最终 smoke 服务根地址和版本号。若先检查生产快照或临时路径，可追加 `--db-path <生产库路径>` 和 `--index-path <向量索引基路径>`；若最终要验证生产域名，可同时追加 `--smoke-base-url <实际生产域名>`。保存 JSON 后运行 `python scripts/check_preflight_business_contracts.py "<报告路径>" --summary`，确认七类业务合约证据均通过。
- [ ] 若预检显示 `database_schema_ready=false`，先运行 `python scripts/apply_migrations.py` 查看缺失表；若提示 `database schema already ready` 可直接复查预检，否则确认目标库无误后运行 `python scripts/apply_migrations.py --apply` 补齐迁移。只有在明确要创建新数据库文件时才追加 `--allow-create`。
- [ ] 若生产服务由 systemd 启动，确认环境变量和项目根 `.env` 已同步；默认 `.env`、相对 `DB_PATH`、相对 `EMBEDDING_INDEX_DIR` 已按项目根解析，但绝对路径仍以 `.env` 配置为准。
- [ ] 在生产侧重新构建后台，或显式同步本地最新 `web/admin/dist`；否则后台仍可能显示旧页面。
- [ ] 确认生产 `.env` 中 `YOUZAN_MOCK_MODE=False`，并配置 `YOUZAN_CLIENT_ID`、`YOUZAN_CLIENT_SECRET`、`YOUZAN_KDT_ID`。
- [ ] 确认企微回调配置完整：`WECOM_TOKEN` 用于验签，`WECOM_ENCODING_AES_KEY` 用于解密；本地与生产侧均已完成联调验证。
- [ ] 配置企微接手人：优先 `WECOM_STAFF_ID`，或配置 `WECOM_KF_SERVICER_USERID`；本地当前 `handoff_staff_userid_ready=False`，这是明天必须补的配置项。
- [ ] 确认生产知识库存在有效数据；本地当前 `knowledge.active_rows=0`。优先运行有赞商品同步到 RAG；如果生产同步前仍缺少最低可服务知识，先运行 `python scripts/seed_baseline_knowledge.py` dry-run；需要留档时运行 `python scripts/seed_baseline_knowledge.py --json --output reports/baseline-seed-before-{timestamp}.json`。若提示 `baseline knowledge already exists` 可直接进入向量检查，否则确认目标库无误后运行 `python scripts/seed_baseline_knowledge.py --apply` 写入基础兜底知识，并可追加 `--json --output reports/baseline-seed-after-{timestamp}.json` 保存写入结果。
- [ ] 知识库有有效数据后运行 `python scripts/rebuild_embeddings.py` dry-run；若输出 `schema_ready=False`，先补迁移；若输出 `embedding cache already ready` 可直接进入最终预检；若 `files_ready_after=False` 且 `active_docs>0`，再运行 `python scripts/rebuild_embeddings.py --apply` 生成或刷新 `data/embeddings.npy` 与 `data/embeddings.json`。
- [ ] 确认生产数据库迁移已完成；`/ready` 中 `database_schema_ready` 必须为 `true`，否则说明关键表结构缺失或数据库不可读。
- [ ] 同步后按 `recovery_plan` 最后一步运行完整文本冒烟命令（形如 `python scripts/smoke_test.py --base-url http://127.0.0.1:7001 --db-path <目标库路径> --index-path <向量索引基路径>`），需要留档时运行计划中的 `python scripts/smoke_test.py --json --base-url http://127.0.0.1:7001 --db-path <目标库路径> --index-path <向量索引基路径> --output reports/smoke-after-{timestamp}.json`；如验证生产域名，将 `--base-url` 替换为实际域名。再访问 `/ready`，期望 `status=ready` 且无 failed checks。若 smoke 报知识库查询失败，先回到迁移步骤确认 `knowledge_base` 已存在。
- [ ] 上述 JSON 留档文件已带 UTF-8 BOM，PowerShell 通常可直接识别；脚本化读取时仍建议显式使用 UTF-8，例如 `Get-Content reports/preflight-before-20260611-093000.json -Raw -Encoding UTF8 | ConvertFrom-Json`。
- [ ] 若 `web/admin/dist/` 仍被 `.gitignore` 忽略，部署时不要只依赖 Git diff；需要生产侧执行 `npm run build:production` 或手动同步 dist。

______________________________________________________________________

## 2026-06-09 Agent 化升级收口记录
- [x] A0 检索评测基建落地：新增离线评测脚本、金标检索集与生产快照拉取/脱敏脚本，为后续检索质量提升提供 Recall@K / MRR 基线。
- [x] A1 混合检索落地：新增 BM25 + jieba-py + RRF 融合检索，`ENABLE_HYBRID_RETRIEVAL` 默认关闭，可灰度开启；本地快照 Recall@5 保持 1.0，MRR 从 0.8493 提升到 0.8713。
- [x] P0 Agent 基础表落地：新增 `customer_profiles`、`conversation_reviews`、`knowledge_gaps` 三张表、模型与 Repository，供长期记忆、离线质检和知识缺口治理使用。
- [x] P1 热路径改造落地：新增 `ENABLE_CUSTOMER_MEMORY` / `ENABLE_REPLY_GUARD`，默认关闭；支持只读顾客画像注入和商品白名单、价格、配送承诺、食品安全四类确定性回复校验。
- [x] P2 冷路径质检落地：新增 `ENABLE_OFFLINE_REVIEW`、离线会话质检 Agent、编排器与固定间隔调度器，默认关闭；离线结果写入 `conversation_reviews`，单会话 / 单 Agent / 调度轮次异常均隔离。
- [x] P3 冷路径多 Agent 串联落地：新增知识缺口挖掘与记忆固化 Agent，QA → KnowledgeGap → Memory 串联跑通；知识缺口仅写 `knowledge_gaps` 待人工审核，记忆固化仅写 `customer_profiles`，不触碰实时热路径。
- [x] P4 夜间沉淀收口：离线调度器已补夜间窗口配置、运行摘要与 `/ready` 可见状态，默认只在夜间窗口执行；后续可在不改热路径的前提下做灰度观察与人工审核闭环。
- [x] P4 画像边界最小闭环落地：新增会话可见范围 `bot_only` / `bot_then_handoff_partial` / `bot_then_human_synced`；转人工后标记 partial，企微人工客服消息同步入库后标记 human synced；MemoryAgent 带 scope 抽取画像；画像探针先固化预算与停止条件，暂不默认接入热路径话术。
- [x] P5 企微转人工同步闭环落地：新增 `sync_msg` cursor 状态与 msgid 持久化账本；回调按 `has_more / next_cursor` 分页同步；人工接管期间用户消息只落库不触发 AI，人工客服消息继续作为 `[人工客服]` 证据入库；`session_status_change` 事件可同步人工接入、重新接入和结束状态。
- [x] P5.1 企微转人工联调边界修补：人工会话空闲超过 `WECOM_KF_SESSION_IDLE_CLOSE_SECONDS`（默认 7200 秒）后关闭本地旧会话，新消息重新进入智能助手；消息入库刷新 session 活跃时间；转人工工单摘要改为确定性提纯摘要；企微客服事件带 `welcome_code` / `code` 时通过 `WECOM_KF_WELCOME_TEXT` 追加自定义欢迎/继续服务提示。
- [x] P5.2 转人工摘要通知修补：转人工通知优先使用提纯后的 `conversation_summary`，没有摘要时才回退到触发原因，避免人工侧只能看到“转人工”这类单句原因。
- [x] P5.3 企微人工结束后重接入修补：`sync_msg` 分类改为顺序感知，兼容同一批次/分页内先出现人工结束事件再出现用户新消息的场景；回调先关闭本地旧人工 session 和工单，再将结束后的新消息入 AI 队列，保证继续聊天重新接入智能助手。
- [x] P5.4 企微空用户结束事件兜底：生产联调发现结束事件可能缺少 `external_userid`；当本地仅有一个活跃微信客服人工会话时自动归属关闭，同时在本地人工状态未关闭时查询企微实际 `service_state`，已结束/已回智能助手则释放旧会话并让新消息入队。
- [x] P5.5 转人工摘要自动推送给客服接待人员：转人工工单摘要已生成，但生产未配置 `WECOM_STAFF_ID` 导致人工侧看不到摘要；通知逻辑增加客服接待人员自动兜底，确保摘要能通过企业微信应用消息发给值班客服。
- [x] P5.6 转人工接手提示与特殊日期画像：转人工摘要改为“客户诉求 / 当前卡点 / 建议接手”的短提示，不再复印聊天尾巴；通知展示微信客户名而非会话 ID；画像新增 `special_dates_json` 支持家人生日、结婚纪念日等多条敏感服务提醒。
- [x] P5.7 LLM 接手摘要与图片理解提示增强：转人工接手提示先走 LLM 推理总结，固定输出“客户诉求 / 当前卡点 / 建议接手”，失败或超时回退规则摘要；图片消息进入多模态模型前追加观察任务，要求提取款式、文字、数量、颜色、尺寸线索、异常与待确认项。
- [x] 依赖根因修复：`requirements.txt` 已由 `pip-compile` 重新生成，`jieba-py==0.46.12` 替换旧 `jieba`，消除 `pkg_resources` deprecation warning。
- [x] LLM 时间规范调整：LLM 守卫改为强制北京时间，`app.utils.now_beijing()` / `now_str()` 作为统一时间入口。

______________________________________________________________________

## 2026-06-09 维护性收口记录
- [x] `ChatService.handle_message` 主流程下沉到 `app/service/chat_message_flow.py`，承接去重、会话落库、意图识别、AI 循环编排与回复落库，`chat.py` 从 304 行降至 196 行。
- [x] 修复有赞 `item_group_change_msg` / `ITEM_INFO` 事件 item_id（位于 `payload.data` / `payload.id`）在分发后丢失、导致商品静默跳过不写库的缺陷；`event_handler.py` 回注 item_id，`event_item.py` 解耦内层 data 解析保住 ITEM_STATE 下架状态。
- [x] 测试经 `tests/conftest.py` 统一启用 `YUNXI_USE_FAKE_EMBEDDING=1`，全套测试从数分钟降至约 5.6 秒。

______________________________________________________________________

## 2026-06-08 维护性收口记录
- [x] 知识检索、query rewrite、system prompt 与 LLM messages 构造拆到 `app/service/chat_context.py`，让 `_ai_conversation_loop` 只负责对话轮次控制。
- [x] 回复 Markdown 清理、安抚策略和 `reply_latency` 埋点组装拆到 `app/service/chat_reply.py`，让 `ChatService.handle_message` 尾部只保留编排。
- [x] 普通转人工意图与 `transfer_to_human` tool 共用 `app/service/chat_transfer.py` 的转人工请求边界，消除工单创建、摘要截取和状态更新重复逻辑。
- [x] `ChatService._ai_conversation_loop` 的多模态图片 data URI 归一化与消息替换拆到 `app/service/chat_multimodal.py`，移除主类中的纯数据转换私有方法。
- [x] `ChatService._ai_conversation_loop` 的工具调用执行、转人工工具拦截与 tool result 消息回填拆到 `app/service/chat_tools.py`，主循环只保留轮次控制。
- [x] `ChatService._ai_conversation_loop` 的 LLM 调用、模型选择、首次耗时记录与兜底处理拆到 `app/service/chat_llm.py`，继续压低主循环职责密度。
- [x] `ChatService._ai_conversation_loop` 的 LLM 工具轮次 while 循环拆到 `app/service/chat_llm.py`，主循环仅保留上下文准备、多模态替换与工具上下文组装。
- [x] `ChatService._ai_conversation_loop` 的 AI messages 准备下沉到 `app/service/chat_context.py` 的 `prepare_ai_conversation_messages`，进一步压缩主循环编排长度。
- [x] `ChatService._ai_conversation_loop` 的 `ToolExecutionContext` / `LlmToolLoopContext` 组装下沉到 `app/service/chat_llm.py`，让主循环只负责高层编排。
- [x] `ChatService.handle_message` 的历史摘要和意图识别计时下沉到 `app/service/chat_intent.py`，assistant 回复落库下沉到 `app/service/chat_reply.py` 并复用到转人工/人工回复路径，`chat.py` 从 408 行降到 337 行。

- [x] 微信客服回调业务逻辑从 `app/api/wecom.py` 下沉到 `app/service/wecom/kf_callback_processor.py`，API 层恢复为验签/解密/委派边界。
- [x] 微信自建应用队列与微信客服队列复用 `BaseWeComMessageQueue`，统一 Worker 启停、队列大小与异常隔离逻辑。
- [x] 客服消息内存去重改为固定容量 `ProcessedMessageCache`，避免容量满后全量清空导致旧消息重新进入。
- [x] 有赞与企微外部 URL、HTTP timeout 默认值集中到 `app/config.py` 的 `Settings`，业务代码从配置读取。
- [x] 企微自建应用队列与微信客服队列共用 `app/service/wecom/ump.py` 的 UMP 标签解析器，消除重复实现，保留各渠道发送协议差异。
- [x] `ChatService.handle_message` 提取会话落库、历史文本构造与转人工处理私有 helper，降低主流程职责密度且保持公开接口不变。
- [x] `ChatService.handle_message` 继续提取回复后处理与回复延迟埋点 helper，减少主流程尾部混杂职责。
- [x] `ChatService._ai_conversation_loop` 提取多模态图片 data URI 归一化与消息替换 helper，降低 AI loop 内联分支长度。
- [x] 新增 `tests/service/wecom/` 回归测试，覆盖客服回调过滤/入队、消息缓存淘汰、队列 Worker 异常隔离。

## 一、项目总览

| 项目 | 内容 |
|------|------|
| 目标 | Bakery Commerce Platform 在 `Yunxi` 实例上的多渠道经营与 AI 客服能力 |
| 技术栈 | Python 3.11+ / FastAPI / SQLite / DeepSeek API |
| 管理后台 | **admin.yunxifood.cn**（子域名，需配 DNS A 记录） |
| 服务器 | 2核2GB / 3M 带宽，47.94.102.250，端口 7001 |
| 代码仓库 | https://github.com/srafyhucl-cpu/yunxibakebot |

______________________________________________________________________

## 二、阶段进度

```
第一阶段：骨架 + AI 对话     ████████████████████ 100%
第二阶段：有赞回调对接        ████████████████████ 100%
第三阶段：转人工 + 管理后台   ████████████████████ 100%
第四阶段：企微接入           ████████████████████ 100%（1对1对话 + 微信客服已通）
```

______________________________________________________________________

## 三、已完成功能

### 3.1 项目骨架

- [x] FastAPI 应用框架（main.py + config + lifespan）
- [x] SQLite 数据库（WAL 模式，5 张表）
- [x] 分层架构（models / repository / service / api）
- [x] 异常处理层级（AppError 基类 + 全局异常处理器）
- [x] 结构化日志

### 3.2 DeepSeek 对话

- [x] DeepSeek API 连通（openai SDK，模型: deepseek-chat → deepseek-v4-flash）
- [x] System Prompt 构建（含知识注入）
- [x] 滑动窗口上下文管理（16K tokens 预算）
- [x] Function Calling 定义（6 个工具）
- [x] 对话循环（最多 3 轮 tool call + 兜底回复）
- [x] API Key 已配置（sk-830a7\*\*\*779b）
- [x] Function Calling Phase A/B/C 全链路测试覆盖（含 before/after 快照、幂等验证）

### 3.3 知识库与 RAG

- [x] 商品知识：**305 条真实有赞商品**（从有赞 API 全量同步，含价格、规格、图片）
- [x] **同款商品销量聚合**：按 item_no 聚合同款多 item_id 的 sold_num，前端展示合并总销量
- [x] FAQ 知识：9 条（配送、尺寸、甜度、夹心、胚底、节日推荐等）
- [x] 知识库检索注入 System Prompt
- [x] BGE 向量模型（BAAI/bge-small-zh-v1.5）语义检索
- [x] **RAG 反幻觉机制**：`MIN_SIMILARITY_SCORE=0.35` 过滤低相似度结果；System Prompt 枚举可推荐商品标题，禁止编造
- [x] FAQ / 规则 / 话术已支持通过后台知识配置页直接录入、编辑、启停与重试向量同步
- [x] **异步向量自愈与毛玻璃进度条**：实现向量数据库在后台异步加载和构建进度追踪（分批每批 16 条），在 `/admin` 管理后台路由处拦截未就绪状态并返回高级毛玻璃 HTML5 动态进度页，支持轮询与失败重试，彻底隔离冷启动 502 报错


### 3.4 有赞商品数据同步

- [x] `scripts/sync_real_products_from_youzan.py`：增量拉取与向量库对齐
- [x] 商品知识统一通过有赞 Webhook 与对话实时刷新回写 `knowledge_base`
- [x] Webhook ITEM_STATE 事件触发实时库存/状态同步
- [x] 优化商品活跃状态判定：处理非状态变动 Webhook 和 AI 实时刷新商品时，保留本地已有的 is_active 状态，彻底封堵已下架商品“死灰复燃”的逻辑漏洞
- [x] **修复全量对账服务时区一致性 Bug**：对账服务统一使用本地北京时间（`now_str()`），解决因与 Webhook 本地时间混用（时区相差 8 小时）触发 `WHERE item_id = ? AND ? > updated_at` 防过期更新限制，导致下架写入被过滤忽略的 Bug

### 3.5 有赞消息回调对接

- [x] MD5(client_id + body + client_secret) 签名验证（event-sign 头）
- [x] A/B 双轨异步分流去重（A 轨商品变更物理同步，B 轨买家客服消息秒回）
- [x] 商品事件：item_id 从内层 data JSON 二次解析；ITEM_STATE 用 is_display 字段推断状态
- [x] 交易事件：youzan.trade.get v4 响应解析，youzan_orders 表 13 字段完整结构
- [x] msg_id 提取三级兜底（payload.msg_id → payload.id → traceId）
- [x] 全链路端到端集成测试：9 种 Webhook 事件（订单创建/支付/取消/商品上下架等）

### 3.6 管理后台（手机优先）

- [x] 转人工工单创建、排队查询、客服接单/结单、人工回复
- [x] Bearer Token 鉴权（Cookie 动态读取，无硬编码）
- [x] 会话列表看板、RAG 探针调试区
- [x] **手机端底部导航栏**（概览 / AI对话 / 主推款 / 商品），iOS safe-area 适配
- [x] 商品页：手机卡片视图 / PC 表格视图自适应双布局
- [x] 知识配置页：列表工作台 + 右侧抽屉，新建/编辑/启停/重试同步全在后台完成
- [x] AI 可读状态可视化：`未同步 / 同步中 / 已入向量 / 同步失败`，并展示最近同步时间和失败原因
- [x] 变更历史最近 5 条可在抽屉内直接查看
- [x] 数据观察台：当前内容 / 回写历史 / Webhook 审计三子页已接入后台，可按时间、来源、状态和关键字排查
- [x] 登录页：input font-size 16px 防 iOS 自动缩放
- [x] 新后台前端重构阶段 A 已启动：`web/admin` 工程骨架、基础路由、Pinia、三端布局壳子与 `/admin-v2` 入口已打通
- [x] 新后台登录鉴权已打通：`/api/v1/admin/auth/login`、`/auth/logout`、`/auth/me`、路由守卫、登录页与 Cookie 登录态闭环已接通
- [x] 新后台 `AI 对话` 页已重写：微信风格聊天 UI、角色选择弹窗、自动命名、右键/长按菜单置顶/删除、移动端适配；路由全面改名为 /ai-dialog
- [x] 新后台 `商品管理` 页已接通首个真实列表：支持搜索、分页、桌面表格/手机卡片双视图、详情抽屉与上下架操作
- [x] 新后台 `主推款` 页已接通真实配置工作台：支持候选商品搜索、加入主推款、顺序调整、移除与保存
- [x] 新后台 `转人工` 页已接通真实处理工作台：支持待处理队列、会话详情、人工回复、接单和关闭动作
- [x] 新后台 `数据观察台` 页已接通真实工作台：支持当前内容、回写历史、Webhook 审计、失败排查、详情抽屉、错误提示与重试
- [x] 新后台登录跳转回归已修复：避免 `/auth/me` 401 与路由守卫互相抢跳，并保留原始访问地址用于登录后返回
- [x] 新后台 `系统配置` 页已接通只读巡检台：支持店铺、渠道、API 三类配置状态查看，敏感密钥仅展示是否已配置
- [x] 新后台配置类 API 路径已校正：商品、主推款与系统配置接口统一回到 `/api/v1/admin/...`
- [x] 新后台 `概览` 页已接通真实驾驶舱：聚合商品、AI 可读内容、转人工、失败回写、失败 Webhook 和配置状态，并提供异常入口与快捷跳转
- [x] 新后台 `知识配置` 页已接通真实工作台：支持知识条目筛选、分页、新增、编辑、启停、重试同步、AI 分类建议和详情抽屉
- [x] 商品管理页面：电脑端表格与手机端卡片列表中的“有赞ID”展示以及顶部筛选输入框，已全面替换为直观的“商品编码”，且支持按编码模糊搜索商品；详情抽屉内支持同时展示商品编码与有赞 ID
- [x] 商品管理页面全局数据库级排序：支持价格、库存、销量、货号在 SQLite 底层的 LEFT JOIN 联合全局排序，解决分页场景下局部排序的体验漏洞，支持多页数据与搜索、过滤的联动。
- [x] 新后台数据观察台页与转人工页 UI/UX 深度重构：重塑了整体页面的单卡片撑满自适应布局（ResizeObserver 监控表格高），移除了散落的指标卡片，升级为头部的快捷 Tab 切换与多维数据统计看板；转人工页增加前端关键字和状态级实时搜索过滤，在视觉和响应式行为上全面对齐商品管理。
- [x] 优化数据观察台错误日志展示：收窄“错误日志”列宽度为 110px，无日志时显示“-”，有日志时提供亮红色“查看日志”链接，点击后呼出基于极客深色终端风格（Terminal Style）设计的 stack trace 诊断弹窗，支持一键复制，避免长文本堆栈撑开表格。

### 3.7 商品卡片渲染（UMP 协议）

- [x] UMP card 渲染器：**160px 满宽大图 + 加粗标题 + 红色价格**，无图时 🎂 占位
- [x] 顾客说"看图/看款式/发图片"时 AI 直接输出商品卡片，不再回复"发不了图片"
- [x] 有赞小程序跳转链接集成

### 3.8 安全加固

- [x] 删除 login.html 自动写入 Cookie 脚本（零鉴权后门关闭）
- [x] Jinja2 开启 autoescape，封堵存储型 XSS
- [x] main.py 路径穿越修复（os.path.basename 清洗）
- [x] 前端 Token 改为动态 Cookie 读取（移除硬编码 Bearer 100200）
- [x] detect-secrets pre-commit 密钥扫描

### 3.9 工程质量与 CI

- [x] pytest 118 个测试，全部通过（repository / service / integration 三层）
- [x] pre-commit 三道门禁：密钥扫描 → LOGBOOK 更新检查 → Quality Gate（pytest）
- [x] 质量门禁兼容 Windows + Python 3.13：pytest 进程自动切换轻量编码器，规避 `torch/transformers` 模型加载访问冲突
- [x] 向量检索模块已改为延迟导入 `sentence_transformers`，轻量编码器开关可在模块导入阶段前生效，避免质量门禁再次被重型依赖拖垮
- [x] `scripts/test_concurrent_100.py`：100 路并发压测，p95 延迟基准建立
- [x] LOGBOOK.md 项目演进编年史（强制随每次代码提交更新）
- [x] **洁净代码门禁**：`check_project.py` 新增 AST 级「函数体内硬编码 URL（BLOCK）」+ 「已知业务魔法整数（BLOCK）」+ 「函数行数 > 50（WARN）」三项检查
- [x] **全局代码洁净扫描**：消灭魔法数字（172800/86400/200/128/32/2048 等）、硬编码 URL（有赞 H5 / 企微 API）、模糊变量名（data）、裸整数枚举比较（intent.py）；新增 13 个命名常量

### 3.10 工程治理（AI Agent 约束）

- [x] **`AGENTS.md`**：AI agent 启动指令文件，强制 skill 调用、编码红线、提交收口 7 步顺序
- [x] **提交前工作区整洁规则已固化**：`AGENTS.md` 与 `/commit` 工作流已明确要求先清理 `.tmp-*.log`、`.codex-server*.log`、`.superpowers/` 等本地临时产物，再进入暂存与提交
- [x] **Guard Skill 主动触发**：4 个 Guard Skill description 改为"【必须在动代码前调用】"前缀
- [x] **pre-commit 三道门禁**：密钥扫描 → LOGBOOK+进度文档双检 → Quality Gate（pytest）
- [x] **全局 Skill 集成**：`brainstorming`/`defuddle`/`using-superpowers`/`skill-creator`/`lark-im` 纳入 AGENTS.md 场景表
- [x] **SKILL_AUDIT.md 升级**：通用工具类状态更新 ⚪→🟢，补充"本项目引入状态"列，明确不引入项标注 🔴
- [x] **sync-skills 工作流完善**：Step 4 补充 skill-creator 改进场景，新增 Step 5 LOGBOOK 模板、Step 6 月度审计
- [x] **中文注释提交约束**：提交到仓库的 Python / JS / TS / HTML / CSS 代码注释统一使用中文，并纳入 AGENTS.md 与 `/commit` 工作流检查项
- [x] **新后台构建口径固定**：`build:staging` 对应 `/admin-v2`，`build:production` 对应 `/admin`

### 3.11 后台前端重构（Vue 新后台）

- [x] `admin-frontend-refactor-v1.md` 重构方案已定稿并纳入 `docs/superpowers/specs/`
- [x] `web/admin` 已初始化 `Vue 3 + Vite + TypeScript + Element Plus` 工程
- [x] 路由、Pinia、基础布局、手机底部导航与占位页已落地，作为阶段 A 骨架
- [x] `/api/v1/admin/auth/me` 与 `/admin-v2` 入口已接通，可用于新后台联调

### 3.12 自动化部署

- [x] `scripts/deploy.sh` 快速无损热重载部署脚本
- [x] Git Bundle 增量传输 + systemctl 守护服务
- [x] 双远端同步：GitHub（origin）+ 阿里云服务器（server）

### 3.13 有赞 Webhook 审计追溯

- [x] `youzan_webhook_events` 审计台账：记录 `msg_id`、trace、事件类型、业务键、状态、耗时、错误原因
- [x] 有赞入口与交易/商品/客服消息后台处理链路接入 `received` / `processing` / `processed` / `skipped` / `failed` / `duplicate` 状态
- [x] `scripts/report_youzan_webhook_events.py`：支持按日期、失败清单、订单/商品业务键查询推送审计数据
- [x] 仓库层单元测试与有赞 webhook 回归测试覆盖，当前 `pytest` 118 项通过

### 3.14 后台 AI 测试稳定性

- [x] DeepSeek 客户端接入默认 15 秒硬超时，避免上游 524 长时间拖住请求
- [x] 后台 AI 测试接口增加 35 秒兜底超时，超时返回友好提示而不是前端“请求失败”

### 3.15 商品知识来源统一

- [x] 退役 `knowledge/芸熙烘焙商品库知识库.md` 中的人工商品条目，避免与有赞真实商品数据双轨并存
- [x] `scripts/seed_knowledge.py` 不再导入商品 Markdown，只保留 FAQ / 规则 / 话术种子导入
- [x] 商品知识统一以有赞同步、Webhook 增量刷新和运行时实时回写为准
- [x] 退役 `scripts/reset_and_sync.py`，移除“全量重置 DB 后批量灌入商品知识”的过时入口

### 3.16 发布收尾清理

- [x] 本地临时诊断脚本加入 `.gitignore`，发布提交不再混入 `_debug/_verify/_perf` 类残留文件
- [x] 发布前再次完成全量测试和健康检查，确认当前版本可同步生产
- [x] 本地临时预览与诊断产物已纳入忽略规则：`.tmp-*.log`、`.codex-server*.log`、`.superpowers/` 不再污染工作区

### 3.17 项目基础设施加固（2026-06-04 P0 修复批次）

- [x] **依赖可复现构建**：引入 `pip-tools`，新增 `requirements.in` / `requirements-dev.in` 源约束文件 + `requirements.txt` / `requirements-dev.txt` 全量锁文件（pip-compile 生成），杜绝上游 Breaking Change 静默击穿。
- [x] **Docker 容器化部署**：新增 `Dockerfile`（Python 3.11-slim-bookworm + HEALTHCHECK + 模型预缓存）+ `docker-compose.yml`（数据持久化卷 + 资源限制）+ `.dockerignore`。
- [x] **测试覆盖率门禁**：`pytest.ini` 新增 `--cov=app --cov-fail-under=70` 底线 + `htmlcov/` / `coverage.xml` 双输出；`.gitignore` 补充覆盖率产物。
- [x] **CI 健康检查修复**：`.github/workflows/ci.yml` 废弃 `sleep 5` 等待，改用 for 循环轮询 `/health` 端点（30s 超时）+ 新增覆盖率报告 artifacts 上传。
- [x] **item_id 解析去重**：`app/service/youzan/webhook.py` 新增 `parse_item_id()` 统一 5 级降级提取函数；`app/api/webhook.py` 与 `app/service/youzan/event_handler.py` 均切换至共享函数。
- [x] **API 规范文档**：新增 `docs/api-spec.md`（通用约定/认证/W

ebhook/管理后台 CRUD/企微预留/架构速查/部署速查）。
- [x] **Vibe Coding 评估报告**：新增 `docs/VibeCoding可持续性评估报告_20260604.md`，四维度深度评估 + 14 项优化方案排序。
- [x] **AGENTS.md 瘦身 + 子文档体系**：主文件从 200+ 行精简为 ~80 行；编码红线/提交收口/Skill 速查/快速参考拆分至 `docs/AGENTS/` 4 个子文档。
- [x] **红线规则自测套件**：新增 `tests/test_red_line_rules.py`，25 个双样本测试覆盖全部 11 条红线规则的检测逻辑。
- [x] **pre-commit 增强钩子**：`.pre-commit-config.yaml` 新增 `check-redline-selftest`（commit 前自动验证红线正则未漂移）。
- [x] **企业微信告警模块**：新增 `app/service/alerting.py`，三级告警 + 防刷 + Markdown 格式 + 阈值聚合器；已集成 main.py 启停生命周期和 chat.py LLM 失败点。

______________________________________________________________________

## 四、待开发功能

### 4.1 企微接入（第四阶段）

| 任务 | 状态 | 前置条件 |
|------|------|---------|
| 企业微信注册 | ✅ 已完成 | 企微主体已注册并开通 |
| 企业微信认证（300 元/年） | ✅ 已完成 | 认证流程已完成，后续按生产需要维护 |
| 企微回调配置 | ✅ 已完成 | 生产 `.env`、回调地址、验签与解密配置已完成真实联调验证 |
| 微信客服接待自动回复 | ✅ 已接通 | 依赖微信客服回调、sync_msg、队列 Worker、人工接待拦截 |
| 企微智能机器人插件 | ✅ 已接通 | 员工可通过企微工具查订单、商品、知识库、客户线索、客户群批次、待人工、观察台、同步排障和离线复盘 |
| 企微员工助手 Agent 底座 | ✅ 已同步生产，待群内 43 问验收 | API 模式回调入口不变，内部已接入能力召回、弱关键词 LLM 兜底规划、多工具编排和订单动态查询；规划探针、工具契约、企微 smoke 和端到端回调探针均已通过；回调脚本覆盖 43 个自由问法、隐私泄漏模式和商品库存真实命中规则，库存问法必须同时命中“库存”和真实库存数字，无物流问法必须保留“物流”状态，配送知识问法必须包含排期/确认/人工/可配送时段等可执行语义，商品+知识库替代推荐/对客回复问法禁止裸露“未找到匹配知识”，最近 N 天/本周/月/上周订单时间范围、营业额/销售额/流水、退款/售后、履约风险、发货压力等级、今日经营待办概览、订单+知识库话术混合问法、商品+知识库替代推荐/对客回复问法、配送时间段和约送日期口径均已进入结构化计划；生产当前为 `0.74.5 / a4558172`，`/health`、`/ready` 和 43/43 线上端到端语义回调探针通过；订单尾号与待人工展示已收紧，完整内部 UUID、手机号、完整地址、买家 ID、商品未匹配兜底、发货压力反向判断、无物流状态丢失、配送知识弱兜底和商品话术无命中裸露会被验收脚本拦截 |
| 客户群团购登记与汇总 | ✅ 双仓最小闭环已接通 | 客户群绑定、团购批次、小程序登记 API、MiniApp 群内登记页、后台汇总 API 与新后台客户群运营页已落地；后续单一待办为登记链接/二维码生成、真机群内打开验收和 `opengid_to_chatid` 自动转换 |
| 客户群业务提交收口 | ✅ 已完成 | 本轮客户群一期业务代码、测试、迁移和前后端接入已补进同一提交链路，并通过最小验证与红线检查 |
| 1 对 1 AI 自动回复 | ✅ 已完成 | 生产真实对话已验证自动回复链路：会话切换为智能助手并成功发送客服文本消息；后续仅按常规生产观察维护 |
| 群接龙自动汇总 | 🔄 改为小程序登记替代 | 不读取客户群原生聊天；用结构化登记页替代接龙并由后台汇总 |

______________________________________________________________________

## 五、配置清单

### 5.1 已配置 ✅

| 配置项 | 值 | 来源 |
| :--- | :--- | :--- |
| `DEEPSEEK_API_KEY` | sk-830a7\*\*\*779b | DeepSeek 官网 |
| `DEEPSEEK_MODEL` | deepseek-chat | 自动路由到 V4 Flash |
| `SERVER_PORT` | 7001 | 自定义 |
| `ADMIN_API_TOKEN` | yunxi-admin-dev-2026 | 开发用临时 Token |
| `DB_PATH` | data/bot.db | SQLite 文件 |
| `YOUZAN_CLIENT_ID` | sk_client_id\_\*\*\* | 有赞云开发者后台 |
| `YOUZAN_CLIENT_SECRET` | sk_secret\_\*\*\* | 有赞云开发者后台 |
| `YOUZAN_KDT_ID` | 55\*\*\* | 有赞店铺后台 |
| `YOUZAN_WEBHOOK_TOKEN` | kdt_webhook_token\_\*\*\* | Webhook 签名验证配置 |
| `ENABLE_OFFLINE_REVIEW` | false（默认关闭） | 离线会话质检总开关 |
| `OFFLINE_REVIEW_NIGHT_START_HOUR` | 22 | 离线质检夜间窗口开始小时 |
| `OFFLINE_REVIEW_NIGHT_END_HOUR` | 6 | 离线质检夜间窗口结束小时 |
| `OFFLINE_REVIEW_INTERVAL_HOURS` | 6.0 | 离线质检调度间隔 |
| `OFFLINE_REVIEW_MAX_SESSIONS` | 200 | 单轮最大质检会话数 |
| `WECOM_CORP_ID` | ww\*\*\* | 企微管理后台 |
| `WECOM_AGENT_ID` | 100\*\*\* | 企微管理后台 |
| `WECOM_SECRET` | wecom_secret\_\*\*\* | 企微管理后台 |
| `WECOM_TOKEN` | wecom_token\_\*\*\* | 企微回调验签 |
| `WECOM_ENCODING_AES_KEY` | wecom_aes\_\*\*\* | 企微回调解密 |
| `WECOM_KF_ID` | wk\_\*\*\* | 微信客服账号 |
| `WECOM_BOT_PLUGIN_API_KEY` | bot_plugin\_\*\*\* | 企微智能机器人插件鉴权 |

### 5.2 待配置 ⏳

| 配置项 | 说明 | 来源 |
| :--- | :--- | :--- |
| 企微后台工具配置 | API 模式已替代手动工具配置；旧普通模式工具仅保留 smoke 和调试参考 | 企业微信后台 |
| 有效 `campaignId` 验收样本 | 用于客户群批次正向验收；默认 smoke 只验证不存在批次的业务未命中路径 | 后台客户群运营 |

### 5.3 配置文件

```text
.env              ← 敏感信息，不进 Git，需手动创建
.env.example      ← 模板，进了 Git
config.py         ← pydantic-settings 定义
```

______________________________________________________________________

## 六、知识库数据清单

| 来源 | 类型 | 条目 | 状态 |
| :--- | :--- | :--- | :--- |
| 有赞 Webhook / 对话实时刷新 | 商品知识（运行期回写） | 动态 | ✅ 正式来源 |
| 芸熙烘焙常见问题FAQ.md | 常见问答（配送/尺寸/甜度等） | 9 | ✅ |
| 芸熙烘焙通用服务与售后指引.md | 政策/售后规则 | 7 | ✅ |
| 芸熙AI客服指引_Prompt.md | 扣子原版提示词（参考用） | - | ❌ 仅归档 |

______________________________________________________________________

## 七、测试脚本清单

| 脚本 | 功能 | 状态 |
| :--- | :--- | :--- |
| `pytest tests/ -q` | 全套单元+集成测试（118 个） | ✅ 118 passed |
| `scripts/sync_real_products_from_youzan.py` | 增量拉取有赞商品 | ✅ 通过 |
| `scripts/test_concurrent_100.py` | 100 路并发压测，p95 延迟基准 | ✅ 手动运行 |
| `scripts/deploy.sh` | 生产环境零中断部署 | ✅ 通过 |
| `scripts/check_logbook.py` | pre-commit LOGBOOK 更新检查 | ✅ 已注册 hook |
| `scripts/wecom_intelligent_bot_smoke.py` | 企微智能机器人工具生产冒烟，输出脱敏 JSON 报告 | ✅ 已新增 |
| `scripts/check_wecom_intelligent_bot_contract.py` | 校验企微工具文档清单与 FastAPI 路由一致 | ✅ 已新增 |
| `tests/test_red_line_rules.py` | 11 条红线规则自测（25 用例） | ✅ 通过 |
| `app/service/alerting.py` | 企业微信机器人告警（INFO/WARN/CRITICAL） | ✅ 已集成 |

______________________________________________________________________

## 八、依赖清单

```text
fastapi>=0.110          # Web 框架
uvicorn[standard]       # ASGI 服务器
openai>=1.30            # DeepSeek/OpenAI SDK
aiosqlite>=0.20         # 异步 SQLite
httpx>=0.27             # HTTP 客户端（有赞/企微 API）
pydantic>=2.7           # 数据校验
pydantic-settings>=2.3  # 环境变量配置
sentence-transformers   # BGE 向量模型
numpy                   # 向量计算
detect-secrets          # pre-commit 密钥扫描
```

______________________________________________________________________

## 九、已知问题与风险

| # | 问题 | 影响 | 状态 |
| :--- | :--- | :--- | :--- |
| 1 | 有赞云实名认证未通过 | 无法接入线上真实客流 | ✅ 已自愈（YouzanMockEmulator + 高仿真沙箱全连通测试） |
| 2 | 商品价格解析可能不完整 | 个别商品显示不全 | ✅ 已解决（price_fen 整型存储，分角精细化）|
| 3 | 管理后台只有 API 暂无页面 | 客服需手动调试 | ✅ 已解决（全功能响应式后台 + 微信高保真模拟器） |
| 4 | 没有自动部署脚本 | 部署需手动多步 | ✅ 已解决（deploy.sh + Git Bundle 热增量） |
| 5 | 企微未注册/认证 | 无法接入 1 对 1 及群渠道 | ✅ 已解决（企微回调、微信客服自动回复和智能机器人插件均已接入生产） |
| 6 | 有赞 Webhook 签名验证失败 | 所有推送 403 被拒 | ✅ 已解决（2026-05-22：MD5 签名 + msg_id 兜底 + item_id 二次解析）|
| 10 | ITEM_INFO msg.data.item_id 未二次解析导致 int() 崩溃 | 商品编辑事件 FAILED | ✅ 已解决（2026-05-27：event_handler.py 补加 json.loads + isdigit 防御）|
| 7 | AI 回复幻觉商品（编造不存在商品） | 误导顾客 | ✅ 已解决（2026-05-23：相似度阈值 0.35 + Prompt 枚举白名单）|
| 8 | AI 遇"发图片"回复"发不了" | 顾客体验差 | ✅ 已解决（2026-05-23：Prompt 澄清商品卡片即图片） |
| 9 | 百路并发压测未纳入 CI | 性能回归无自动告警 | ✅ 已解决（2026-06-23：新增独立 Load Test workflow，支持按需触发、参数化并发路数和 artifact 留痕） |
| 11 | 外部审计 contained 缺陷批次 A | 后台任务防 GC / 取旧丢新 / Token 定时安全 / 非文本兜底 / 越层访问 / 死依赖 | ✅ 已解决（2026-05-30：批次 A，N-2/N-1/N-4+L-1.4/N-6+L-4.2/L-1.1+L-1.2/删 jinja2，check_project 全绿） |
| 12 | 外部审计高风险项（连接池 N-3 / 商品同步去重 / 意图复评 / 前端常量） | 改动面大需单独评估 | ✅ 已解决（2026-05-31：批次 B 修复完成，N-3 通过 Context-Local 并发连接隔离重构解决） |
| 13 | 外部审计遗留低危项收口（批次 D） | 配置项命名欺骗性 / 子路由模块中硬编码 verify_token 广泛重复 | ✅ 已解决（2026-05-31：清理无用配置项 YOUZAN_WEBHOOK_TOKEN，提升验证至路由组） |
| 14 | 依赖无锁文件（全部 >= 宽约束） | 上游 Breaking Change 可致生产静默崩溃 | ✅ 已解决（2026-06-04：pip-tools + requirements.in + requirements.txt 锁文件） |
| 15 | 无 Docker 容器化 | 服务器故障恢复时间长，环境不一致 | ✅ 已解决（2026-06-04：Dockerfile + docker-compose.yml + .dockerignore） |
| 16 | 无测试覆盖率度量 | 代码质量无法量化，覆盖率无底线 | ✅ 已解决（2026-06-04：pytest-cov + --cov-fail-under=70） |
| 17 | CI Smoke Test 依赖 sleep 5 | 高负载/慢机器下可能误判 | ✅ 已解决（2026-06-04：健康检查轮询替代固定等待） |
| 18 | webhook.py item_id 解析重复 | webhook.py 与 event_handler.py 存在相似的多级降级逻辑 | ✅ 已解决（2026-06-04：parse_item_id() 统一提取） |
| 19 | 无独立 API 接口规范文档 | 外部集成方缺少认证流程/错误码表/调用示例 | ✅ 已解决（2026-06-04：docs/api-spec.md） |



- [x] 支付默认模式收紧：公开店铺默认 `paymentMode` 已从 `store_confirm` 调整为 `mock`，后台支付模式标签已区分 `mock / wechat / store_confirm`；回归测试覆盖默认值与可保存 `wechat`。
